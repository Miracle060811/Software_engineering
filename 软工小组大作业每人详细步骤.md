### 软工小组大作业

### 一、 核心亮点与系统构思（Idea）

按照题目要求，平台需要覆盖**“行前规划 - 行中服务 - 行后分享”**的全场景。为了拿到高分和加分项，建议系统命名为**“伴游（TravelMate）”**，并设计以下亮点：

1.  **AI智能行程规划（行前 - 强力加分项）**：接入大模型，用户只需输入“预算3000、去大理玩3天、带老人”，AI自动生成包含机酒推荐、每日路线的行程单。
2.  **出行票务与酒店预订（行前/行中 - 核心基本盘）**：机票/火车票与酒店的搜索、比价、下单闭环（模拟支付即可）。
3.  **AI贴心客服Agent（行中 - 加分项）**：一个基于Agent的智能助手，解答“当地天气如何”、“行李丢了怎么办”、“附近有什么好吃的”。
4.  **旅途游记社区（行后 - 核心基本盘）**：类似小红书的图文分享，支持打卡、定位、点赞评论。
5.  **超级管理员可观测仪表盘（后台 - 加分项）**：直观展示当日订单量、热门目的地Top10、系统报错日志等。

---

### 二、 团队分工建议（5人制）

根据要求：**每人至少负责1个核心需求，4个模块，每个模块至少5个主要功能**（即每人要细化出至少20个功能点）。

#### 👨‍💻 成员 A（组长/核心）：“大交通”票务系统（核心需求1）
*   **定位**：负责跨城出行预订。
*   **模块拆分**：
    1.  **机票预订模块**：航班查询、多维度筛选（航司/时间）、余票显示、退改签规则展示、预订下单。
    2.  **火车票模块**：车次查询、席别选择（一等/二等）、中转方案推荐、乘车人管理、下单扣座。
    3.  **价格比对模块**：历史价格趋势图、降价订阅提醒、多平台比价模拟、优惠券领取、特价日历。
    4.  **订单中心模块**：票务订单列表、订单状态流转（待支付/已出票/已取消）、模拟支付接口、退款申请、行程单下载。

#### 👩‍💻 成员 B：酒店与目的地服务系统（核心需求2）
*   **定位**：负责住宿与本地服务。
*   **模块拆分**：
    1.  **酒店搜索模块**：按地标/商圈搜索、条件过滤（星级/价格/设施）、地图找房（可选）、酒店详情页、房型展示。
    2.  **房务预订模块**：入住/离店日期选择、房间数量管理、住客信息填写、防超卖逻辑、生成预订单。
    3.  **本地玩乐模块**：景点门票查询、一日游产品推荐、周边游展示、门票预订、扫码核销（生成模拟二维码）。
    4.  **评价评分模块**：订单完成后的星级评价、图文评价上传、标签选择（如“干净卫生”）、商家回复展示、评价举报。

#### 👨‍💻 成员 C：AI行程规划与Agent助手（核心需求3 + 加分项）
*   **定位**：负责AI大模型接入与智能服务（需单独撰写Markdown说明Prompt和Skills）。
*   **模块拆分**：
    1.  **智能规划模块（行前）**：表单输入需求、调用大模型API生成行程、行程可视化展示（每日路线）、行程单导出/分享、行程一键转购物车。
    2.  **AI客服Agent模块（行中）**：自然语言对话框、基于上下文的意图识别、调用天气/航班查询Tools（Skills）、多轮对话历史管理、转人工客服逻辑。
    3.  **消息通知模块**：航班延误预警提示、酒店入住提醒、系统站内信、红点未读提示、消息批量已读/删除。
    4.  **行程管理模块**：我的行程列表、行程日期倒计时、行程拖拽修改、行程关联订单跳转、同行人共享行程。

#### 👩‍💻 成员 D：旅途社区与用户中心（核心需求4）
*   **定位**：负责UGC内容生态与用户基础体系。
*   **模块拆分**：
    1.  **动态发布模块**：图文上传（支持多图）、富文本编辑、关联话题/地点、发布权限设置（公开/私密）、草稿箱。
    2.  **社区瀑布流模块**：首页推荐流、关注者信息流、动态搜索、按照热度/时间排序、动态分享。
    3.  **互动社交模块**：点赞、收藏、二级评论树（回复评论）、关注/取消关注用户、粉丝列表查看。
    4.  **个人主页模块**：用户注册登录（JWT鉴权）、个人信息编辑（头像/昵称/签名）、我的发布/收藏/点赞记录管理、密码修改/找回。

#### 👨‍💻 成员 E：超级管理后台与可观测系统（核心需求5 + 加分项）
*   **定位**：负责B端管理后台与系统监控，保障系统可用性。
*   **模块拆分**：
    1.  **可观测仪表盘模块**：大屏数据展示（折线图/饼图等）、今日订单总数统计、系统QPS/延迟监控模拟、热门目的地热力图、异常预警通知。
    2.  **资源管理模块**：航班库管理（增删改查）、酒店库录入与上下架、房态/票量库存干预、价格修改、促销活动配置。
    3.  **内容审核模块**：社区图文敏感词过滤（基础算法或调API）、人工审核队列、动态屏蔽/删除、用户封禁处理、申诉处理。
    4.  **平台财务/用户模块**：全平台订单流水查看、退款人工审批、平台注册用户画像查看、角色权限控制（RBAC：超级管理员/普通运营）。

*(注：上述模块拆分完全契合“每人至少4个模块、每个模块5个功能点”的要求，且覆盖了所有基本项和加分项。)*

---

### 三、 开发步骤与时间线（严格对齐截止时间）

### 第 1-6 周：基建、调研与原型设计（兵马未动，粮草先行）
这几周虽然没有文档提交硬指标，但决定了你们项目的基础质量。

*   **第 1-2 周：需求调研与竞品分析**
    *   **核心动作**：全员注册携程、小红书。截图它们的核心链路（机票怎么搜、酒店怎么定、游记怎么发）。
    *   **数据准备**：真实数据太难搞，建议用大模型（如Kimi/ChatGPT）生成一份包含 50个航班、50家酒店、100条用户评价的 Mock 数据（JSON格式），存入数据库备用。
*   **第 3-4 周：技术选型与基建搭建（开发同学主导）**
    *   **代码仓库**：建立 GitHub / Gitee 仓库，约定分支规范（`master`为主分支，每个人建自己的`feature/xxx`分支）。
    *   **接口工具**：强烈建议使用 **Apifox** 或 **YApi**。前端和后端尽早定好数据格式（JSON），后端哪怕还没写逻辑，先返回假数据，让前端能开工。
    *   **AI准备**：C同学去申请智谱GLM或通义千问的免费API Key，写几个简单的 Python/Node.js 脚本测试连通性。
*   **第 5-6 周：UI/UX 原型设计（加分项发力点）**
    *   **核心动作**：不要直接写前端代码！使用 **Figma** 或 **墨刀** 画出核心页面的线框图（不需要很精美，但要统一样式、颜色和交互逻辑）。
    *   **组件库**：前端敲定UI框架（如 Element Plus, Vant, Arco Design），这是拿到“美观设计加分项”的最快捷径。

---

### 第 7 周：计划阶段（提交《软件开发计划书》）
*   **主要任务**：向“甲方（老师/助教）”证明你们有靠谱的落地计划。
*   **文档必写内容**：
    1.  **里程碑计划**：列出清晰的甘特图（可用Excel或Teambition生成），精确到周。
    2.  **团队分工矩阵（RACI表）**：明确写出A负责交通、B负责酒店、C负责AI等，**在文档里高亮说明“每人均负责4个模块，满足大作业要求”**。
    3.  **风险评估**：写上“大模型API调用限流”、“高并发下库存超卖”等技术风险，并给出应对策略（如加缓存、加锁），显得你们很专业。

---

### 第 8 周：需求阶段（提交《需求规格说明书》 SRS）
*   **主要任务**：定义系统到底要做什么（What），不要涉及怎么做（How）。
*   **文档必写内容**：
    1.  **用例图（Use Case Diagram）**：用 Visio 或 Draw.io 画。区分普通用户、管理员两种角色。
    2.  **功能需求列表**：一定要把功能点拆细！例如“机票预订”要拆成：查询、排序、填乘机人、选座（可选）、生成订单 5个点。凑齐要求。
    3.  **非功能需求**：重点写**“可用性”与“并发稳定性”**（呼应评分标准）。例如：“接口响应时间<1秒，支持50人同时下单的并发压力”。

---

### 第 9-10 周：系统设计（提交《概要设计说明书》 HLD）
*   **主要任务**：定义系统的骨架和数据结构。
*   **重点动作**：**全员开会定数据库 E-R 图！**（极其重要）
    *   如果A写的机票表和B写的酒店表、D写的用户表无法关联，后面联调会死人。必须统一 UserID、OrderID 等核心外键。
*   **文档必写内容**：
    1.  **系统架构图**：画出前端（小程序/Web） -> 网关/Controller -> 服务层 -> 数据库/Redis缓存的层级结构。
    2.  **数据库设计**：给出初步的表结构设计。
    3.  **模块划分图**：清晰展示5个人的负责模块如何协同工作。

---

### 第 11-12 周：详细设计（提交《详细设计说明书》 LLD）
*   **主要任务**：深入到代码级别的设计，此时**后端代码应该已经开始大量编写**了。
*   **文档必写内容**：
    1.  **时序图（Sequence Diagram）**：必须画！挑选几个最复杂的业务画，比如：*从点击“支付机票”到“扣除余票、生成订单、发送站内信”的全流程*。
    2.  **核心算法/逻辑设计**：C同学负责的AI模块，在这里要写清楚：Prompt 是怎么设计的？Function Calling (Skills) 是怎么抽取的？D同学的推荐信息流是怎么排序的（比如按时间+点赞数加权计算）？
    3.  **接口契约**：列出关键 API 的 Request 和 Response 结构。

---

### 第 13-14 周：冲刺编码、联调与压力测试（生死时速）
文档交完了，全面进入代码冲刺和测试阶段。

*   **前后端联调**：打破部门墙，A前端连A后端，跑通后大家把代码合并到一起。
*   **扣库存的并发问题（针对助教验收）**：
    *   评分标准里写了“保证在小并发量下的稳定性”。助教大概率会用多开浏览器或者简单脚本测试**“同时抢最后一张机票/一间房”**。
    *   **应对方案**：后端必须加锁（最简单的是数据库乐观锁 `UPDATE tickets SET stock = stock - 1 WHERE id = 1 AND stock > 0`，或者用 Redis 分布式锁/Lua脚本）。
*   **准备演示视频与PPT**：
    *   写好**视频剧本**。不要随便录！
    *   按照“行前规划（展示AI） -> 行中订票/定酒店（展示核心业务和稳定性） -> 行后发游记（展示社区） -> 后台数据监控（展示仪表盘加分项）”的顺滑逻辑录制。
    *   如果遇到Bug，千万别录进去，视频必须丝滑。

---

### 第 15 周：验收交付与答辩（临门一脚）
*   **提交物清单**：
    1.  **代码包**：清理掉 `node_modules` 和 `target` 这种大体积编译文件。提供一份 `database.sql` 供助教一键导入假数据。
    2.  **部署文档**：**写得越傻瓜越好！**明确标出环境要求（JDK 1.8 / Node 16 / MySQL 8.0），一步步截图教助教怎么启动系统。最好能写一个 `start.bat` 或 `docker-compose.yml` 一键启动。
    3.  **测试报告**：附上你们自己做的黑盒测试用例截图，以及 JMeter 并发测试无报错的截图。
    4.  **单独撰写的 Markdown 附件（保命+加分项）**：
        *   **标题**：《大模型技术应用与辅助编码说明》
        *   **内容 1 (AI业务应用)**：展示你们系统中 AI Agent 的 Prompt 怎么写的，Skills（如查询天气的函数）的代码片段是怎么封装的。
        *   **内容 2 (AI辅助编码留痕)**：附上小组成员让大模型帮忙写 Vue 组件、写 SQL 语句的 **对话截图 (Session)**，证明你们“合理利用且完全理解”了AI的代码，而非抄袭开源项目。
*   **答辩现场提示**：PPT第一页就把你们的**“人员分工与功能点对应表”**亮出来，直接告诉评委你们完美满足了硬性指标，剩下的时间全用来演示加分项（AI规划、可观测仪表盘、UI美观）。

---

文档中有一条非常严厉的警告：**“严禁任何形式的抄袭！包括照搬现成的开源项目在内……过度的借鉴可能适得其反。”**

因此，我给你们的建议是：**千万不要去 GitHub/Gitee 上搜索“旅游平台开源项目”然后直接拿来改！** 助教查重系统很容易查出你们和往届或者现有项目的相似度。

最聪明、最安全的做法是：**使用成熟的“底层基础脚手架”（免去写登录、权限、后台界面的痛苦），然后参考“电商类开源项目”的业务设计思维（将买商品转换为买机票/酒店），最后自己编写核心业务代码。**

以下是为你们小组精选的参考框架和开源项目，分为四个维度：

---

### 一、 强烈推荐的底层基础框架（直接拿来做底座，不违规）

这类项目叫做“脚手架”，它们只提供系统最基础的功能（如登录、注册、角色权限、菜单管理）。你们在它的基础上开发业务，不仅不会被判抄袭，反而会显得架构很专业。

#### 1. 若依系统（RuoYi-Vue）—— ⭐️⭐️⭐️⭐️⭐️ (国内大作业/企业项目万能神框)
*   **技术栈**：Spring Boot + Vue3 + Element Plus + MySQL + Redis
*   **适用模块**：极其适合**成员E（超级管理员后台）**直接使用。
*   **为什么推荐**：
    *   它自带了极其完善的RBAC（基于角色的权限控制）系统，超管和普通商家的权限隔离直接就有了。
    *   **逆天功能：代码生成器。** 你们在数据库建好“机票表”、“订单表”，点一下鼠标，它能自动生成前后端完整的增删改查（CRUD）代码。这能帮你们省下至少60%写重复代码的时间，把精力全放在核心逻辑和加分项上。
*   **地址**：Gitee 搜索 `RuoYi-Vue3`

#### 2. litemall (又一个小商场系统) —— ⭐️⭐️⭐️⭐️
*   **技术栈**：Spring Boot + Vue + 微信小程序
*   **适用模块**：适合作为**用户端（小程序）**的基础参考。
*   **为什么推荐**：它的代码结构非常清晰，包含了一个基础的移动端商城。你们可以**把里面的“商品”改造成“酒店/景点门票”，“购物车”改造成“行程单”**。它的微信登录封装、Token校验逻辑非常值得借鉴。
*   **地址**：Gitee/GitHub 搜索 `litemall`

---

### 二、 核心业务逻辑参考项目（只看架构和表设计，自己写代码）

出行平台本质上是一个**“带有库存和时间属性的电商平台”** + **“内容社区”**。

#### 1. 复杂电商逻辑参考：mall (macrozheng)
*   **作用**：**成员A（大交通）**和**成员B（酒店预订）**必看！
*   **参考点**：重点看它的数据库设计！机票和酒店最难的是“库存扣减”和“订单状态流转”。去看看 `mall` 项目是怎么设计商品SKU（把每一天的酒店房间当成一个SKU）、怎么处理订单超时未支付自动取消的（利用消息队列或定时任务），这正是助教要考查的“高并发/稳定性”考点。
*   **地址**：GitHub 搜索 `macrozheng/mall`

#### 2. 社区瀑布流与社交参考：lin-cms 或各类“仿小红书”前台模板
*   **作用**：**成员D（旅途社区）**参考。
*   **参考点**：去 Gitee 或 Dcloud 插件市场搜索“仿小红书 瀑布流”，看看前端是如何用 CSS 实现双列瀑布流的，以及后端的点赞、收藏数据表是如何设计的（通常是建立关联表，例如 `user_like_post` 表）。

---

### 三、 UI 美观设计加分项（前端组件库）

为了拿到文档中提到的“UI/UX美观设计”加分项，千万不要自己手写原生CSS，一定要用现成的精美组件库：

#### 1. 移动端/小程序端组件库
*   **uView UI** (适用于 Uni-app)：目前生态最好的多端 UI 框架，里面的卡片、标签、骨架屏、瀑布流组件极其丰富，做出来的页面非常有现代感。
*   **ColorUI**：一个极其注重视觉美感的 CSS 库，提供了非常多好看的渐变色和阴影设计，随便用一用就能让界面档次提升（特别适合旅游这种需要展示美景的平台）。

#### 2. 超级管理员仪表盘（数据可视化）
*   **ECharts**：必须用！**成员E**在做可观测性仪表盘时，直接去 ECharts 官网的“示例”里，找几个折线图（展示每日订单趋势）、饼图（展示热门目的地）的代码复制进去，改一下数据源，高级感瞬间拉满。

---

### 四、 AI 大模型与 Agent 参考框架（核心加分项）

**成员C（AI行程规划与Agent）** 的技术难点在于如何把大模型接入系统。

#### 1. ChatGPT-Next-Web (或者叫 NextChat)
*   **作用**：这是一个开源的跨平台 ChatGPT 网页端。
*   **参考点**：你们不需要照抄它，但成员 C 必须去看看它的前端是怎么处理**“打字机效果（流式输出 Stream）”**的。如果你们的 AI 规划需要等 10 秒钟才一次性蹦出结果，体验会很差；如果能像 ChatGPT 一样一个字一个字往外蹦，绝对是高分表现。

#### 2. 智谱 GLM 或 Kimi 的官方 SDK 文档
*   这比任何开源项目都管用。现在这些国产大模型的 API 文档写得极其傻瓜，直接提供 Java/Python/Node 接入的代码片段。
*   **重点研究“Function Calling (函数调用/Skills)”**：这就是文档要求里写的 `Skills`。去看看怎么让大模型知道你们数据库里有哪些航班，当用户问“明天去北京的机票”，大模型能通过 Function Calling 调用你们自己写的查询接口，然后把结果返回给用户。

---

### 💡 给你们小组的落地执行建议

1.  **后端基建**：今天就可以让组里后端最强的同学去下载 **RuoYi-Vue3**，在本地跑起来，把数据库连上。然后把默认的“部门管理”、“岗位管理”隐藏掉，建好你们的“航班表”、“酒店表”、“游记表”，用自带的代码生成器把基础增删改查生成好。**这就相当于大作业完成了30%**。
2.  **前端分工**：确定好使用 Uni-app 写用户端，直接引入 uView 组件库，5个人按照《详细设计文档》的接口开始画页面、连接口。
3.  **如何规避查重**：基础脚手架的代码不会被认定为抄袭（因为全网都在用），但你们**自己建的表、自己写的订单业务逻辑 Controller、Service，绝对不能从网上复制**，必须是自己手敲的。

按照“若依后台管理 + Uni-app多端前台 + 自己写的旅游电商逻辑 + 接入智谱API”的组合，你们的工作量会大大减轻，且成品效果极具企业级项目的“欺骗性”，绝对能拿高分。

