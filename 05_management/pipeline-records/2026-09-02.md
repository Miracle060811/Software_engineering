# 2026-09-02 最终非部署验收记录

## 1. 当前基线

- 工作分支：`main`；检查开始时 HEAD 为 `dde8b329`；
- 已有远端证据：GitHub Actions run `33582799133` attempt 2 success；
- 本轮性能、迁移、管理与非 PPT 答辩材料保存于本次本地验收提交；精确哈希以 `git rev-parse HEAD` 为准。

## 2. 本轮结果

| 检查 | 结果 |
| --- | --- |
| `npm run check:traceability` | 19 完整、0 部分、0 待测、38/38、0 结构错误 |
| `npm run check:microservice-e2e` | 19/19 runnable、0 blocked |
| `npm run check:microservice-api` | 113 端点通过，其中 94 公开、19 内部 |
| 性能实验 | 12/12 k6 运行退出码 0，错误率均为 0，61 个证据文件 |
| 故障实验 | IDENTITY 停止时返回 503，TRAFFIC 存活，订单和库存不变，恢复成功 |
| 数据迁移 | 31/31 张表一致，源库未修改，7/7 临时容器已清理 |

## 3. 迁移脚本缺陷与修复

真实空库预检发现 `Get-TableCount` 将单行结果字符串 `"0"` 先索引为字符，再转为整数 48，导致空表误判。将表达式修正为 `[long](@($rows)[-1])` 后，正式迁移通过。失败运行均在写入目标前停止或由空临时库承载，没有连接现有开发数据库。

## 4. 剩余边界

HPA、单服务真实失败镜像回滚依赖 Kubernetes 环境；答辩 PPT 按用户要求不在本轮范围。个人权重与全员确认、9 月 3 日站会和看板必须由实际成员或在当天补充，不能自动生成完成结论。
