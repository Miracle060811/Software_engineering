# 2026-08-28 数据库与部署门禁实施记录

## 当日汇总

| 项目 | 内容 |
| --- | --- |
| 记录人 | 成员 2 |
| 验证方式 | 本地静态检查与清单渲染 |
| 当前结论 | 本地检查通过；远程 GitHub Actions 待提交后补充 |

## 完成内容

- 将 Flyway 迁移文件命名、版本唯一性、连续性以及禁止创建/切换数据库加入自动检查；
- 检查 Kubernetes 必需资源、前后端及 MySQL/Redis 健康探针、ConfigMap 敏感键和 `latest` 镜像标签；
- 检查 `scripts/cd` 下 PowerShell 部署脚本的语法；
- CI 后端测试改为从空数据库启动，由 Flyway 执行 V1、V2，并核对 `flyway_schema_history`；
- 真实后端 E2E 改为验证已打包 JAR 能自行完成数据库迁移；
- 复核镜像发布门禁、同 commit 前后端镜像配对、digest 部署与失败回滚逻辑；
- 在根 README 补充数据库迁移规则、本地检查命令和 CI 行为。

## 本地验证结果

| 检查项 | 状态 | 证据或说明 |
| --- | --- | --- |
| `npm run check:deployment` | 通过 | Flyway 版本、Kubernetes 资源、健康探针和敏感配置符合规则 |
| PowerShell 部署脚本解析 | 通过 | `scripts/cd/*.ps1` 未发现语法错误 |
| `kubectl kustomize deploy/k8s` | 通过 | 成功渲染完整 Kubernetes 清单 |
| Git diff 格式检查 | 通过 | 未发现空白符错误 |

## 远程流水线记录

本文件不虚构未运行的远程结果。代码提交并触发 GitHub Actions 后，应补充以下信息：

- Commit SHA；
- CI Run ID 与链接；
- `Repository and use-case validation`、`Backend integration tests and package`、`Real backend E2E` 的结果；
- 镜像发布与扫描结果（仅 `main` 上满足发布条件的代码变更会产生）；
- 若部署到演示机，补充部署 commit、镜像 digest、健康检查和回滚情况。
