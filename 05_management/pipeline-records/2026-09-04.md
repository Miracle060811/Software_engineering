# 2026-09-04 正式前端六服务接线修复记录

## 问题与影响

只读复核 Kubernetes、前端 Nginx、Vite 开发代理和 CI 后确认：六个微服务已部署，但正式前端镜像仍把 `/api`、`/user`、`/uploads` 统一代理到 `travelmate-backend:8080`。因此此前的“六服务已部署”和“前端实际使用六服务”不是同一件事，现场从正式前端操作时实际走的是兼容单体。

## 修复内容

- `frontend/nginx.conf` 按接口所有权分别代理 identity、traffic、local、ai、community、ops 六服务，取消单体 fallback；
- `.github/workflows/ci.yml` 恢复 `Real microservice E2E` 必需作业，并加入 `CI quality gate` 与 `CI/CD delivery gate`；
- 该作业构建正式前端 Docker 镜像，通过生产 Nginx 运行 17 个 Playwright 场景，而不是只测试 Vite 开发代理；
- 静态门禁同时检查 Nginx、Vite、Playwright 与 workflow 接线，防止后续回退。

## 本地验证结果

验证基线为提交 `8c23392810831431d1788693e688cd18473a0b77` 加当前未提交修复工作树。

| 验证项 | 结果 |
| --- | --- |
| 六服务 Maven reactor `verify` | 通过，六个模块全部成功 |
| 六服务健康检查 | 8081—8086 全部 `UP` |
| 微服务 API 契约门禁 | 通过，119 个端点（100 公开、19 内部） |
| 微服务边界门禁 | 通过，六服务、36 张业务表唯一归属 |
| Nginx 配置语法 | `nginx -t` 通过 |
| Vite 微服务入口 E2E | 17/17 通过 |
| 正式前端镜像 + 生产 Nginx + 六服务 E2E | 17/17 通过，联合覆盖 UC01—UC19 |

## 证据边界

以上为 2026-09-04 本地隔离 Docker 环境的真实运行结果，不是远程 GitHub Actions Run，也不是已部署到当前 Kubernetes 集群的新版本。修改提交并推送后，仍须以新 commit 对应的 GitHub Actions `Real microservice E2E`、镜像 digest、Kubernetes rollout 和部署后健康检查作为最终 CI/CD 证据。

## 后续：AI 行程迁移与 npm 审计暂时故障修复

远程提交 `c082d226` 的 [CI/CD Run 33840279131](https://github.com/Miracle060811/Software_engineering/actions/runs/33840279131) 中，前后端测试、六服务测试、真实微服务 E2E、真实单体 E2E 和 CI quality gate 全部成功；发布被安全门禁阻止。

根因见 [安全 Run 33840279125](https://github.com/Miracle060811/Software_engineering/actions/runs/33840279125)：`Node.js dependency audit` 在前端审计时请求 npm 官方 `/-/npm/v1/security/advisories/bulk`，返回 `503 Service Unavailable`。本次失败并非发现高危依赖，也不是部署 runner 故障；Secret scan、Maven audit 和该提交 CodeQL 均成功。

修复：新增 `scripts/ci/audit-npm.mjs`，保持 `--package-lock-only --omit=dev --audit-level=high`，仅对明确的临时网络错误重试，最多 3 次（间隔 10/20 秒）；设置请求和进程超时。真实漏洞、鉴权/锁文件错误和无法识别的输出立即失败，重试耗尽仍阻止发布。`security.yml` 对根目录和前端统一使用此脚本，并运行 6 项重试/阻断回归测试。没有降低安全门禁或改成忽略失败。

同批交付包含微服务 AI 行程规划，配置和验证范围见 `microservices/README.md`。AI 单元/HTTP 模拟测试 30 项通过；前端生产构建通过。远程新版本状态需按提交 SHA 查询，不以此处的本地结果代替远程 CI/CD 完成证明。

## 再次修复：npm 审计持续超时

提交 `f18433f4` 的安全 Run `33841881782` 中，重试测试通过，npm 根依赖审计连续三次超时，导致 CI/CD Run `33841881784` 的 Release security gate 失败。全部业务测试和真实 E2E 已通过。

本次增加必须成功的 Trivy Node 生产依赖扫描，复用已固定 SHA 的 Trivy action（默认 v0.70.0），只扫描根目录和前端的 package.json/package-lock.json。扫描 HIGH/CRITICAL，包括尚无修复版本的漏洞；同时验证报告确实覆盖两份锁文件，并保存审计 artifact。npm 仍执行生产依赖审计，但临时错误重试耗尽返回非零专用码 75；只有先前独立扫描已通过时，CI 才将此状态标为注册表不可用。真实漏洞、未知错误、Trivy 失败或报告覆盖不完整均阻断发布。

验证：8 项 Node 回归测试通过；本机使用官方 v0.70.0 Windows 压缩包（核对发布 checksums），下载最新漏洞库并扫描，两份 npm 锁文件被识别，HIGH/CRITICAL 为 0。Trivy 的 Node 锁文件支持见 https://trivy.dev/docs/dev/guide/coverage/language/nodejs/ 。新提交仍需以远程安全门禁结果为准。
