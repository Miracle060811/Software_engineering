# 2026-08-27 数据库迁移验证记录

## 验证范围

- Flyway 依赖与启动配置；
- 空数据库首次初始化；
- 已有数据库基线登记与增量升级；
- 重复启动时的迁移幂等性；
- 升级前后的关键数据保留情况。

## 验证环境

| 项目 | 结果 |
| --- | --- |
| 操作系统 | Windows，本地开发环境 |
| 数据库 | MySQL 8.0 |
| 后端 | Spring Boot 3.5.15 |
| 迁移工具 | Flyway（由 Spring Boot 依赖管理） |
| 迁移文件 | `V1__baseline_schema.sql`、`V2__image_data_update.sql` |
| 敏感信息 | 未写入记录；验证过程从本地 `.env` 读取 |

## 空数据库初始化

使用独立临时数据库 `travelmate_flyway_verify_20260827` 启动后端，不操作现有 `travelmate` 数据库。

结果：

- Flyway 成功校验 2 个迁移文件；
- 成功执行 V1 基线迁移；
- 成功执行 V2 图片数据更新；
- `flyway_schema_history` 中 V1、V2 状态均为成功；
- 初始化后共有 33 张表（包含 Flyway 历史表）；
- 后端能够完成启动并通过数据库连接检查；
- 临时数据库已在验证结束后删除。

## 已有数据库升级

使用 `mysqldump --single-transaction` 将现有 `travelmate` 复制到独立临时数据库 `travelmate_flyway_clone_verify_20260827`，仅在副本上执行迁移。

升级前：

| 核对项 | 数量 |
| --- | ---: |
| 业务表 | 32 |
| `tm_user` | 5 |
| `tm_attraction` | 48 |

升级结果：

- Flyway 将非空旧库登记为版本 1 基线，没有重复执行 V1；
- V2 图片数据更新执行成功；
- 后端能够使用升级后的数据库正常启动；
- 第二次启动显示数据库已是最新版本，无需再次迁移；
- `tm_user` 保持 5 条，`tm_attraction` 保持 48 条；
- 业务表数量保持 32，新增 1 张 `flyway_schema_history`，总数为 33；
- 临时数据库和临时转储文件均已删除。

## 结论

空库初始化、旧库基线升级、重复启动和关键数据保留均通过。本次验证未修改、删除或重建原 `travelmate` 数据库。

## 后续工作

- 将数据库迁移验证加入 GitHub Actions；
- 增加迁移文件命名、版本顺序和重复版本检查；
- 在提交后补充 Commit SHA、CI Run ID 和远程证据链接。
