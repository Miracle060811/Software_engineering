我先基于你当前的建表脚本看一遍现有边界，整理出适合这个项目继续扩展的数据层方向，重点会放在“业务缺口、结构规范、性能与可维护性”三类。

Read [](file:///e%3A/SoftEngneeringHomework/Software_engineering/docs/sql/init.sql#1-1), lines 1 to 260

Read [](file:///e%3A/SoftEngneeringHomework/Software_engineering/docs/sql/init.sql#261-261), lines 261 to 520

现有表已经覆盖了“用户、交通、酒店、景点、社区、AI、运营”几个主模块，但现在更像课程作业版的单体初稿。下一步扩充，最值得做的是把“库存、订单、内容、审计”这几块从演示级提升到可持续迭代级。

Read [](file:///e%3A/SoftEngneeringHomework/Software_engineering/docs/sql/init.sql#521-521), lines 521 to 577

我再定位几个关键表的行号，方便把建议直接对应到现有设计，而不是泛泛而谈。

Searched for regex `` CREATE TABLE IF NOT EXISTS `tm_hotel_room`|CREATE TABLE IF NOT EXISTS `tm_hotel_order`|CREATE TABLE IF NOT EXISTS `tm_attraction`|CREATE TABLE IF NOT EXISTS `tm_review` `` (`**/docs/sql/init.sql`), 4 results

Searched for regex `` CREATE TABLE IF NOT EXISTS `tm_traffic_order`|CREATE TABLE IF NOT EXISTS `tm_price_history`|CREATE TABLE IF NOT EXISTS `tm_notification`|CREATE TABLE IF NOT EXISTS `tm_post` `` (`**/docs/sql/init.sql`), 4 results

Searched for regex `` CREATE TABLE IF NOT EXISTS `tm_coupon`|CREATE TABLE IF NOT EXISTS `tm_user_coupon`|CREATE TABLE IF NOT EXISTS `tm_reply`|CREATE TABLE IF NOT EXISTS `tm_review_report` `` (`**/docs/sql/init.sql`), 4 results

已经拿到关键表位置。下面我会把建议按“优先级”和“为什么值得现在做”整理，尽量贴着你这份库表说。

Searched for regex `` CREATE TABLE IF NOT EXISTS `tm_user`|CREATE TABLE IF NOT EXISTS `tm_flight`|CREATE TABLE IF NOT EXISTS `tm_train`|CREATE TABLE IF NOT EXISTS `tm_passenger` `` (`**/docs/sql/init.sql`), 4 results

Searched for regex `` CREATE TABLE IF NOT EXISTS `tm_comment`|CREATE TABLE IF NOT EXISTS `tm_like`|CREATE TABLE IF NOT EXISTS `tm_follow`|CREATE TABLE IF NOT EXISTS `sys_log` `` (`**/docs/sql/init.sql`), 4 results

我再快速确认一下代码层已经做到了哪些业务，避免建议和现有实现重复。重点看“景点下单、支付、库存、社区标签”等是否已经在代码里单独建模。

Search complete: 核实现有业务覆盖

搜索代理这次没返回结果，我直接用代码搜索补一下，范围只收敛到你准备扩库会受影响的几个模块。

Searched for regex `AttractionOrder|Payment|Refund|RoomCalendar|Inventory|PostTag|PostMedia|Session` (`**/backend/src/main/java/**/*.java`), 16 results

Searched for regex `AttractionOrder|payment|refund|coupon|notification|review report|reply` (`**/frontend/src/**/*.vue`), 80 results

Searched for files matching `**/backend/src/main/java/com/travelmate/entity/*.java`, 24 matches

我补一眼订单和退款相关代码，确认你库表扩展里哪些是“已经有接口但数据模型还不够完整”的地方。

Read [](file:///e%3A/SoftEngneeringHomework/Software_engineering/backend/src/main/java/com/travelmate/entity/TrafficOrder.java)

Read [](file:///e%3A/SoftEngneeringHomework/Software_engineering/backend/src/main/java/com/travelmate/entity/HotelOrder.java)

Read [](file:///e%3A/SoftEngneeringHomework/Software_engineering/backend/src/main/java/com/travelmate/controller/AdminController.java#550-550), lines 550 to 620

我已经确认到一个很具体的扩库切入点：后台有退款审批接口，但目前只是直接改订单状态，没有独立退款单或支付流水表，这一块最适合先补。

Searched for regex `public class TrafficOrder|public class HotelOrder|approveRefund|rejectRefund` (`**/backend/src/main/java/**/*.java`), 8 results

Searched for regex `@TableName\("tm_traffic_order"\)|@TableName\("tm_hotel_order"\)` (`**/backend/src/main/java/com/travelmate/entity/*.java`), 2 results

从你这份库表看，已经把旅游平台的主干业务铺出来了，但现在更偏“能演示”，离“可持续扩展”还差几层关键建模。最值得优先做的，不是继续堆新表，而是把订单、库存、内容这三块补完整。几个明显信号是：

init.sql 的交通订单和 init.sql 的酒店订单目前只有订单主表；init.sql 有景点表，但没有景点订单表；init.sql 的房型只有汇总库存，没有按日期的房态；init.sql 的帖子把图片和标签都直接塞进字符串；而后台退款审批现在只是改订单状态，没有独立退款记录，AdminController.java 和 AdminController.java 就是这个情况。

**优先建议**
1. 先补“订单闭环”。
新增 `tm_attraction_order`、`tm_payment_record`、`tm_refund_record`。你现在酒店和交通订单实体只有主状态，没有支付流水和退款单据，TrafficOrder.java 和 HotelOrder.java 都是这个结构。后面一旦要做“退款审核记录、支付方式、失败原因、重复支付防重、对账”，没有独立流水表会很难维护。

2. 再补“库存日历化”。
酒店最建议新增 `tm_hotel_room_inventory`，字段至少有 `room_id`、`biz_date`、`total_stock`、`locked_stock`、`sold_stock`、`version`。现在 init.sql 的 `available_rooms` 只能表示总余量，不适合处理多日期入住。景点也建议做 `tm_attraction_inventory`，不然只能用总票数减来减去，无法按日期售卖。

3. 社区内容从字符串字段拆出来。
init.sql 里的 `images`、`tags` 用逗号拼接，短期能跑，后面会卡在“标签搜索、热门话题、图片排序、封面图、视频扩展”。建议拆成 `tm_post_media`、`tm_tag`、`tm_post_tag` 三张表。这样社区检索、推荐和统计都会简单很多。

**第二梯队建议**
1. 通知表补“业务指向”。
docs/sql/init.sql 的通知只有 `type/title/content/is_read`，建议加 `biz_type`、`biz_id`、`sender_id`、`action_url`。否则前端通知中心只能展示文本，没法稳定跳转到具体订单、评论、帖子。

2. 用户域补“行为与安全”。
如果后面要做风控或运营，建议加 `tm_login_log`、`tm_user_session`、`tm_verification_code`。当前 init.sql 的用户表更像基础账号表，不够支撑异地登录、验证码找回、登录设备管理这些常见需求。

3. 评论和评价补“可治理字段”。
评论表 init.sql 和评价表 init.sql 可以补 `status`、`audit_status`、`ip`、`device`、`reply_count`、`report_count`。后续做审核、反垃圾、举报处理会更顺。

4. 优惠券补“规则快照”和“使用约束”。
现在 init.sql 和 init.sql 已经有券和用户券，但还缺 `applicable_scope`、`applicable_type`、`per_user_limit`、`coupon_template_id`、`order_id`。否则很难表达“仅酒店可用”“每人限领一张”“某张券到底用了在哪个订单上”。

**结构层建议**
1. 给核心业务表统一补 `update_time`。
现在有些表有，有些没有，后续查审计、排查线上问题会不一致。

2. 给高并发库存表加 `version`。
你们说明里提到过乐观锁，但当前这份 SQL 里多数表没有 `version` 字段。库存和优惠券领取这类场景最好明确建进去。

3. 重要外键即使不加 FK 约束，也至少补索引。
例如：
`tm_traffic_order(user_id, status, create_time)`
`tm_hotel_order(user_id, status, create_time)`
`tm_notification(user_id, is_read, create_time)`
`tm_post(status, visibility, create_time)`
`tm_comment(post_id, parent_id, create_time)`
`tm_flight(departure_city, arrival_city, departure_time, status)`
`tm_train(departure_station, arrival_station, departure_time, status)`

4. 唯一约束再补几处。
例如 `tm_passenger(user_id, id_card)`，避免同一用户重复保存同一乘客；`tm_user_coupon(user_id, coupon_id)` 要看你们是否允许重复领券，不允许的话应做唯一约束。

如果你现在只想选“最值”的三项，我建议按这个顺序做：
1. 支付单 + 退款单 + 景点订单
2. 酒店房态日历表
3. 帖子媒体表 + 标签关系表

如果你愿意，我下一步可以直接按你这个项目现状，给你出一版“扩库后的 ER 结构清单”和对应的 SQL 草案。