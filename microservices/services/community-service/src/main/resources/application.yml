server:
  port: ${SERVER_PORT:8085}
spring:
  application.name: community-service
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://${DB_HOST:127.0.0.1}:${DB_PORT:3306}/${DB_NAME:travelmate_community}?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai}
    username: ${SPRING_DATASOURCE_USERNAME:${DB_USERNAME:root}}
    password: ${SPRING_DATASOURCE_PASSWORD:${DB_PASSWORD:}}
    driver-class-name: com.mysql.cj.jdbc.Driver
  data.redis:
    host: ${REDIS_HOST:127.0.0.1}
    port: ${REDIS_PORT:6379}
app:
  cors.allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://127.0.0.1:3000}
  security.jwt-secret: ${JWT_SECRET:}
  internal-service-token: ${INTERNAL_SERVICE_TOKEN}
  services:
    identity-url: ${IDENTITY_SERVICE_URL:http://127.0.0.1:8081}
    ops-url: ${OPS_SERVICE_URL:http://127.0.0.1:8086}
management:
  endpoints.web.exposure.include: health,info
  endpoint.health.probes.enabled: true
