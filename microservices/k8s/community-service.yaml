apiVersion: v1
kind: Service
metadata:
  name: community-service
spec:
  selector:
    app.kubernetes.io/name: community-service
  ports:
    - name: http
      port: 8085
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: community-service
spec:
  replicas: 2
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: community-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: community-service
        app.kubernetes.io/part-of: travelmate
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: community-service
          image: travelmate/community-service:dev
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8085
          envFrom:
            - configMapRef:
                name: travelmate-microservices-config
          env:
            - name: DB_HOST
              value: community-db
            - name: DB_NAME
              value: travelmate_community
            - name: DB_USERNAME
              value: travelmate_community_app
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: travelmate-microservices-secrets
                  key: community-db-password
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: travelmate-microservices-secrets
                  key: jwt-secret
            - name: INTERNAL_SERVICE_TOKEN
              valueFrom:
                secretKeyRef:
                  name: travelmate-microservices-secrets
                  key: internal-service-token
            - name: JAVA_TOOL_OPTIONS
              value: -XX:MaxRAMPercentage=75.0
          startupProbe: &communityProbe
            httpGet:
              path: /actuator/health/liveness
              port: http
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 36
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            <<: *communityProbe
            periodSeconds: 10
            failureThreshold: 6
          resources:
            requests:
              cpu: 100m
              memory: 192Mi
            limits:
              cpu: 500m
              memory: 384Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}

