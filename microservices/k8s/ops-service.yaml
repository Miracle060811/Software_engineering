apiVersion: v1
kind: Service
metadata:
  name: ops-service
spec:
  selector:
    app.kubernetes.io/name: ops-service
  ports:
    - name: http
      port: 8086
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ops-service
spec:
  replicas: 2
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: ops-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ops-service
        app.kubernetes.io/part-of: travelmate
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: ops-service
          image: travelmate/ops-service:dev
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8086
          envFrom:
            - configMapRef:
                name: travelmate-microservices-config
          env:
            - name: DB_HOST
              value: ops-db
            - name: DB_NAME
              value: travelmate_ops
            - name: DB_USERNAME
              value: travelmate_ops_app
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: travelmate-microservices-secrets
                  key: ops-db-password
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: travelmate-microservices-secrets
                  key: jwt-secret
            - name: INTERNAL_SERVICE_TOKEN
              valueFrom:
                secretKeyRef:
                  name: travelmate-microservices-secrets
                  key: internal-service-token
            - name: JAVA_TOOL_OPTIONS
              value: -XX:MaxRAMPercentage=75.0
          startupProbe: &opsProbe
            httpGet:
              path: /actuator/health/liveness
              port: http
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 36
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            <<: *opsProbe
            periodSeconds: 10
            failureThreshold: 6
          resources:
            requests:
              cpu: 100m
              memory: 192Mi
            limits:
              cpu: 500m
              memory: 384Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}

