# TravelMate 最终非部署验收测试报告（2026-09-02，2026-09-03 补充验收）

## 1. 总结论

本地固定基线 `d31bc6b21728473e15adda64cd243f2cd79530f4` 及当前稳定工作树上，流水线等价复跑、HPA、单服务不可用镜像注入与回滚、性能对比、历史数据迁移和管理后台均已有可复核证据；四名成员已确认个人权重。管理后台修复后真实复测满足 API/UI 退出码均为 0、14 个仪表盘核心字段齐全、匿名与普通用户均被 403 拒绝、浏览器错误为空、临时账号和测试数据清理成功。

本次补充验收严格限于本地工作区与 `docker-desktop` 集群，没有查询远程 Git、GitHub Actions 或下载远程 Artifact。下述本地复跑是对仓库流水线关键门禁的等价验证，不冒充 GitHub Actions 原始运行记录。

## 2. 本地流水线等价复跑

证据目录：`04_tests/pipeline/results/final-local-validation-20260903-d31bc6b2/`；运行编号 `FINAL-LOCAL-20260903-D31BC6B2`。

| 步骤 | 结果 |
| --- | --- |
| UC01—UC19 追溯、119 端点契约、真实 E2E 结构、服务边界、部署/Flyway | 全部通过 |
| 六套分库 Schema 生成与两处交付镜像一致性 | 12/12 文件一致，local-service 16 张表 |
| 部署回滚确定性脚本测试 | 4 个场景通过 |
| 单体 Maven 全测 | 190/190 通过 |
| 六服务 Maven `verify` | 117/117 通过（identity 19、traffic 22、local 33、ai 18、community 11、ops 14） |
| 单体 / local-service UC08 MySQL 集成 | 3/3 + 3/3 通过 |
| 前端 lint、单测与生产构建 | lint/build 通过，单测 26/26 |
| Playwright smoke / UC08 真实 API 与页面 | 6/6 + 2/2 通过 |
| 管理后台与单服务回滚既有证据哈希、字段及清理状态 | 完整性校验通过 |

十八个步骤退出码均为 0，共汇总 313 条 Java 测试（0 失败、0 错误、0 跳过）、归档 75 份 Surefire/Failsafe XML、逐步原始日志与 `SHA256SUMS.txt`；临时 MySQL、Redis、后端和 Vite 进程均已停止。这组证据只证明固定本地 HEAD 加当前协调工作树可重复通过相应门禁，不证明远程平台本日运行状态。

## 3. 云原生实验

### 3.1 HPA

2026-09-02 对 `traffic-service` 实机加压：50 VU 下 CPU 峰值 237%，HPA 期望副本升至 6，实际达到 4 个 Ready Pod；负载结束后恢复至 2 个副本。4252 个 HTTP 请求错误率 0%，平均约 5.09ms、P95 约 7.27ms，k6 退出码 0。正式口径仅引用现存截图，见 `04_tests/stress/results/HPA实验记录-20260902.md` 与 `04_tests/stress/results/evidence/hpa-20260902-complete/`。

### 3.2 单服务失败镜像回滚

2026-09-03 在本地 `docker-desktop` 的 `travelmate-microservices` 命名空间，将 `identity-service` 临时改为不存在的本地镜像并设置 `imagePullPolicy=Never`。集群观察到 `ErrImageNeverPull` 且 rollout 超时退出；脚本随后恢复原镜像与策略，目标服务恢复 2/2 Ready，不可用 Pod 清零。其他服务及数据库、Redis 的 Pod UID、imageID、重启次数保持不变，未尝试远程拉取。结构化结果 `passed=true`，见 `04_tests/fault/results/image-rollback-20260903-175647/`。

## 4. 管理后台复核

证据目录：`04_tests/admin/results/admin-review-20260903/`。

| 检查 | 结果 | 判定 |
| --- | --- | --- |
| 普通用户访问管理接口 | HTTP 403 | 通过 |
| 审核、举报处理、敏感词重复校验、审计日志及清理 | Playwright `E2E-TC-118/E2E-TC-119`，1 条真实 API E2E 通过 | 通过 |
| 管理员登录并进入“内容审核与举报处理/游记审核” | 页面与列表可渲染，截图已保存 | 通过 |
| 仪表盘数据加载 | HTTP/业务码 200，14 个核心字段齐全 | 通过 |

一次性普通用户和管理员已在证据采集后注销；评价、举报、敏感词和管理员日志等临时数据均清理并复核归零；端口转发、Vite 与本机 ops 临时进程均已停止。`result.json` 与 `manifest.json` 均标记 `passed=true`。

## 5. 其他已闭环证据

- 用例追溯：UC01—UC19 完整覆盖、0 部分、0 待测，38/38 自动化证据；
- API 覆盖：119 个端点，其中 100 个公开、19 个内部；
- 性能：航班查询、酒店查询、并发下单三个场景，单体与微服务各 3 轮，共 18 次运行，HTTP 错误率均为 0，并发下单 6 次均未超卖；
- 数据迁移：MySQL 8.4 隔离环境中 31/31 张表行数一致，源库未修改，7/7 临时容器清理；
- 故障隔离：IDENTITY 停止时交通订单返回 503，交通查询仍可用，订单与库存不变，依赖恢复成功。

## 6. 交付边界

技术测试范围内的验收阻塞已经闭环，最终 18 项本地门禁已在文档与数据库生成链稳定后全绿复跑。项目管理证据以 `05_management/` 中的站会、看板和流水线原始归档为准，不由技术测试结果替代。
