# TravelMate 微服务验收差距整改测试报告（2026-09-01—2026-09-02）

## 1. 结论

本轮已完成六服务 API 契约覆盖、UC01—UC19 真实微服务 E2E 门禁、单服务定向部署/回滚脚本及其回归测试。三个整改 PR 均处于 `OPEN`、`MERGEABLE`，PR 门禁全绿；因此当前结论是“代码与 PR 验证完成，尚未合入 `main`”。

不能据此宣称新代码已经完成生产发布：PR 流水线按设计跳过 GHCR 发布与 Docker Desktop Kubernetes 部署。单服务更新/回滚也只通过 fake `kubectl` 回归，尚缺真实集群演练证据。

## 2. 整改项与证据

| 整改项 | 结果 | 精确证据 | 边界 |
| --- | --- | --- | --- |
| T1 六服务 API 契约覆盖 | 通过 | PR [#224](https://github.com/Miracle060811/Software_engineering/pull/224)，提交 `a3439e95b48b9ca9534d3224f79e29f417064914`；113 个端点，其中 94 个公开端点、19 个内部端点；本地 Maven 100 项测试无失败；[CI run 33579487206](https://github.com/Miracle060811/Software_engineering/actions/runs/33579487206) 全绿 | PR 尚未合并 |
| T2 六服务真实 E2E 门禁 | 通过 | PR [#225](https://github.com/Miracle060811/Software_engineering/pull/225)，提交 `d85048fdef679bbabd7a5d0cd7c6bfe3fcfae523`；[CI run 33580242791](https://github.com/Miracle060811/Software_engineering/actions/runs/33580242791) 的 `Real microservice E2E` 为 17/17 通过，覆盖 UC01—UC19；六服务独立 JAR、六个 Schema、真实 MySQL/Redis 均参与运行 | PR 尚未合并；17 个场景并非 19 个独立测试，一些场景联合覆盖多个 UC |
| T2 失败修复 | 通过 | 首次 run `33579497229` 为 16/17；UC01 浏览器登录返回 `403 Invalid CORS request`。修复 CI 的 `CORS_ALLOWED_ORIGINS=http://127.0.0.1:3100`，并将该配置纳入结构回归检查；重跑 17/17 | 修复仅放宽 CI 专用 Origin，不改变默认业务环境白名单 |
| T2 证据归档 | 通过 | Artifact `microservice-e2e-evidence-9c1a1f41f81be313a6b5b37fb523dc601f496179`，246761 bytes，未过期；包含 Playwright 报告、失败追踪目录及服务日志上传路径 | GitHub Artifact 保留期为 30 天 |
| T4 单服务独立更新/回滚 | 代码与回归通过 | PR [#226](https://github.com/Miracle060811/Software_engineering/pull/226)，提交 `c655dbd06de5bb789cb3d89001b6ce0f6f2ac7a3`；`-Service` 只修改目标 Deployment 的 image、annotation、rollout 与 rollback；fake `kubectl` 回归验证其他五服务不变；[CI run 33579505031](https://github.com/Miracle060811/Software_engineering/actions/runs/33579505031) 全绿 | 未在真实 Kubernetes 集群执行目标服务更新和失败回滚 |

本轮本地结构检查：

- `npm run check:microservice-e2e`：UC01—UC19 结构覆盖 19/19，runnable 19，blocked 0。
- `npm run check:traceability`：完整覆盖 19、部分覆盖 0、尚待测试 0、结构错误 0，自动化证据分 38/38。

## 3. 已有 main 发布与 Kubernetes 证据

整改前的 `main` 发布基线为 commit `04059bfc187d4bb28184f6f2f88d1e75f1481c91`。GitHub Actions [run 33488234482](https://github.com/Miracle060811/Software_engineering/actions/runs/33488234482) 第 4 次尝试为 `success`。

### 3.1 六服务不可变镜像

| 服务 | 部署镜像摘要 |
| --- | --- |
| identity-service | `ghcr.io/miracle060811/travelmate-identity-service@sha256:b54eec8ace1700c990ae168d434504bb21d61d9b440baaab5226bcc6215cfbb6` |
| traffic-service | `ghcr.io/miracle060811/travelmate-traffic-service@sha256:d43b653970832c94d67d1b3985b05a7c83cfad55ce1f1e7f20711da273b78d69` |
| local-service | `ghcr.io/miracle060811/travelmate-local-service@sha256:c753bb8e7d82898c1074b5a4b18db47595bfce2bdbbd31c21a287bb3ec74c79d` |
| ai-service | `ghcr.io/miracle060811/travelmate-ai-service@sha256:2b52fac00833783b15c71a846fa631b68690fdea19d48fca9dd9c18b50471789` |
| community-service | `ghcr.io/miracle060811/travelmate-community-service@sha256:7bdfd4b7348fdf750983f67918fd483a8f1567c943ed6d94bf5149f00f4b16ed` |
| ops-service | `ghcr.io/miracle060811/travelmate-ops-service@sha256:d77cb835ca75ff3c22ed6bbd69765cf69bb8e79f0d9416ab0d547c5d3925b067` |

### 3.2 集群状态

第 4 次尝试上传的 `kubernetes-deployment-04059bfc187d4bb28184f6f2f88d1e75f1481c91` 证据显示：

- 六个微服务 Pod 均为 `1/1 Running`，六服务容器重启数合计为 0。
- 8081—8086 六个健康端点均返回 HTTP 200。
- MySQL、MinIO 与 uploads 三个 PVC 均为 `Bound`。
- 单体 backend 与 frontend 健康端点均返回 HTTP 200。

该证据只证明 commit `04059bf` 的集群部署，不证明 PR #224—#226 的提交已经部署。

### 3.3 失败尝试记录

同一 run 的第 1、2、3 次尝试均失败在自托管 runner `YFan_deploy` 的源码拉取步骤：`git fetch` 在 5 分钟后出现 `fatal: fetch-pack: invalid index-pack output` 并超时，随后 delivery gate 正确报告 `deploy=failure`。第 4 次尝试成功完成拉取、部署、摘要核对和健康检查；没有通过放宽门禁掩盖失败。

## 4. 当前未完成项

| 未完成项 | 完成条件 |
| --- | --- |
| 合并整改代码 | 审阅并按依赖顺序合并 PR #224、#225、#226；当前未获合并授权 |
| 新 `main` 六服务发布 | 合并后 `main` 流水线的安全门禁、六镜像发布、release evidence、Docker Desktop Kubernetes 部署全部成功 |
| 单服务真实更新 | 在可用 `kubectl` context 上只更新一个服务，保存更新前后六服务 imageID、Pod UID、rollout status 和健康结果 |
| 单服务真实回滚 | 注入一个不可用目标镜像，证明仅目标服务失败并回滚到原 digest，其他五服务 Pod UID/imageID 不变 |
| HPA/性能实证 | 准备 Metrics Server 与 k6，保存 HPA、Pod、CPU 和负载输出；本报告不把静态 HPA YAML 当成运行实证 |

## 5. 合并后复验命令

```powershell
npm run check:traceability
npm run check:microservice-e2e
npm run check:deployment
npm run check:api-coverage
kubectl config current-context
```

真实集群演练应使用合并后的 `scripts/cd/Deploy-TravelMateMicroservices.ps1 -Service <service>`，并将输出写入独立 evidence 目录；不得用 fake `kubectl` 结果替代真实集群证据。
