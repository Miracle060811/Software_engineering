@startuml
|用户|
start
:提交航班或车次查询条件;
|TravelMate|
if (存在符合条件的班次?) then (是)
  |用户|
  :选择舱位/席别、乘客和优惠券;
  |TravelMate|
  if (身份、乘客和优惠有效?) then (是)
    if (库存充足?) then (是)
      :扣减库存并创建待支付订单;
    else (否)
      if (火车票且用户选择候补?) then (是)
        :创建候补记录;
        stop
      else (否)
        :返回库存不足;
        stop
      endif
    endif
  else (否)
    :返回校验失败原因;
    stop
  endif
else (否)
  :返回无班次结果;
  stop
endif
|用户|
:确认模拟支付;
|TravelMate|
if (15 分钟内完成支付?) then (是)
  :更新为已支付并生成凭证;
else (否)
  :关闭订单并回补库存;
  stop
endif
|用户|
if (后续操作?) then (正常履约)
  |TravelMate|
  :更新为已出行;
else (取消或退款)
  |TravelMate|
  if (状态允许且未重复处理?) then (是)
    |管理员|
    :按规则审核退款;
    |TravelMate|
    :退款并回补库存;
  else (否)
    :拒绝非法或重复操作;
  endif
endif
stop
@enduml