# 软件需求规格说明书

## 1 范围
### 1.1 标识
- 系统名称：伴游（TravelMate）出行旅游平台
- 缩略词：TravelMate
- 版本号：V1.1
- 编制日期：2026-08-25
- 开发小组：Miracle 开发小组

### 1.2 系统概述
“伴游(TravelMate)”是一款面向出行旅游场景的一体化平台，旨在围绕“行前规划——行中服务——行后分享”的完整旅程为用户提供服务。系统主要功能包括：
资源预订：机票、火车票购买，酒店住宿及本地玩乐预订。
智能服务：基于大语言模型的AI行程规划及AI客服Agent。
社交分享：旅途社区游记分享与互动。
管理端：后台资源维护、内容安全审核及系统可观测性监控。
本项目为软件工程基础课程模拟项目，强调核心需求覆盖、功能可用性、小并发稳定性以及文档一致性。

### 1.3 文档概述
本文档描述“伴游”平台的 CSCI 需求及验收条件，作为系统设计与测试的基础。文档包含用户故事与需求编号、完整用例图、UC01—UC11 用例说明、领域概念类图、逐用例系统级模型规划，以及功能、性能、环境、安全性和外部接口需求，适用于开发团队、教师及助教。

### 1.4 基线
《软件开发计划书》V1.0 
《TravelMate 核心需求描述文档》 
《软件概要设计说明书》 
《项目构思文档》 

## 2 引用文件
《软件工程基础2026春大作业要求》 
《软件开发计划书》V1.0(Miracle小组) 
《软件详细设计说明书》(Miracle小组) 
DeepSeek API官方技术文档 
Spring Boot, Vue3, MySQL官方技术规范

## 3 需求
### 3.1 所需的状态和方式
系统需支持以下运行状态：
正常运行状态：用户可进行资源检索、AI行程生成、下单支付及社区发布。
离线/维护状态：后台进行资源批量更新或数据库维护。
降级模式：在大模型API响应超时时，系统需提供本地Mock数据支持或错误重试提示。

### 3.2 ==需求概述==
#### 3.2.1 目标
核心目标：解决传统旅游平台信息破碎、行程规划繁琐的问题，利用AI技术实现结构化、个性化的旅程定制。
处理流程：用户输入需求->AI生成结构化行程->匹配本地Mock资源->下单预占库存->模拟支付->订单履约->行后社区分享。
系统定位：作为课程驱动的模拟软件，不接入真实支付与票务接口，采用“业务闭环真实、外部依赖模拟”策略。
#### 3.2.2 运行环境
客户端：支持Chrome、Edge等主流Web浏览器。
服务端：Java 21, Spring Boot 3.x。
数据库：MySQL 8.0, Redis缓存。
#### 3.2.3 用户的特点
普通游客：需要直观的资源搜索和一键式AI行程生成，对交互效率有要求。
内容创作者：使用社区发布工具进行多图上传和位置打卡。
平台管理员：进行资源管理、内容审核及系统运行状态实时监控。
#### 3.2.4 关键点
AI Agent集成：利用DeepSeek API实现JSON结构化输出，通过结构化Prompt工程与多轮对话上下文管理实现个性化行程规划与智能客服问答。
库存防超卖：采用“Redis 缓存预减+数据库乐观锁”策略保证订票订房场景的并发安全。
状态机控制：严格管理订单从“待支付”到“已出行”的状态跃迁，防止状态乱序。
#### 3.2.5 约束条件
开发期限：2026年4月至6月，共9周。
技术栈限制：必须采用Vue 3, Spring Boot 3.x, MySQL, Redis等指定栈
经费限制：课程模拟项目，预算有限，优先使用免费及公开API。

### 3.3 ==需求规格==
#### 3.3.1 软件系统总体功能/对象结构
系统分为表现层(Vue 3)、业务服务层(Spring Boot)及数据持久层(MySQL/Redis)。
#### 3.3.2 软件子系统功能/对象结构
系统划分为五个核心子系统：大交通票务子系统：机票/车次查询、历史价格走势图、候补抢票、优惠券抵扣、订单生命周期管理。
目的地住宿与本地生活子系统：酒店多维搜索、房务库存控制、景点门票预订与订单凭证、五星评价体系（含标签、举报与商家回复）。
AI智能规划与Agent服务子系统：行程JSON生成、AI多轮客服问答、全局通知服务。
旅途社区与用户中心子系统：JWT认证、UGC图文发布、瀑布流社交关系链。
管理后台与可观测性子系统：RBAC权限、操作日志统计大屏、敏感词过滤审核、退款审核工作流、CSV批量数据导入、用户封禁管理。
#### 3.3.3 描述约定
时间标准：统一采用create_time, update_time字段名。
命名规范：数据库字段采用小写下划线，代码变量采用camelCase。

### 3.4 CSCI 能力需求

#### 3.4.1 用户故事与需求编号

为保证业务场景可以从需求一路追溯到设计、代码和测试，本项目采用 `REQ-xx / UCxx / SYS-UCxx / COMP-UCxx / OBJ-UCxx / UNIT-TC-1xx / INT-TC-1xx / E2E-TC-1xx` 的统一编号方式。下表中的“应”表示必须满足的验收要求。

| 需求编号 | 用户故事 | 验收要求 | 对应用例 |
| :--- | :--- | :--- | :--- |
| REQ-01 | 作为游客或用户，我希望安全地注册、登录和维护账户，以便使用个性化及受保护功能 | 系统应使用密码哈希和 JWT；错误凭据、失效 Token、禁用账号和越权请求必须被拒绝 | UC01 |
| REQ-02 | 作为出行用户，我希望完成航班或火车票的查询、预订和履约，以便获得明确的订单或候补结果 | 系统应支持乘客选择、优惠校验、库存扣减、支付、取消、退款和无票候补，库存不得为负 | UC02 |
| REQ-03 | 作为订单用户，我希望订单按合法状态完成支付、取消、退款或履约 | 系统应校验订单归属和状态跃迁，取消、超时和退款时应正确回补库存 | UC02、UC03、UC04 |
| REQ-04 | 作为住宿用户，我希望完成酒店查询、订房和订单处理，以便获得可查询的住宿结果 | 系统应校验入住日期、房型与库存，并保证并发订房不超卖 | UC03 |
| REQ-05 | 作为本地游用户，我希望购买景点票或预订本地游产品，以便获得购票或预订结果 | 系统应返回票种、价格、可售状态和订单结果；失败时不得产生脏订单 | UC04 |
| REQ-06 | 作为消费用户，我希望在订单中使用优惠券并在履约后评价，以便获得优惠并反馈体验 | 系统应支持领券、门槛校验、一次性核销、评价、回复和举报 | UC02—UC05 |
| REQ-07 | 作为旅行用户，我希望获得 AI 行程和连续问答，以便快速形成可执行方案 | AI 输出应结构化并可保存；外部 AI 失败时应降级，不得编造实时库存或订单状态 | UC06、UC07 |
| REQ-08 | 作为用户，我希望接收通知和站内私信，以便及时了解业务变化并与他人沟通 | 系统应正确维护未读状态和会话消息，并阻止越权访问 | UC08 |
| REQ-09 | 作为内容用户，我希望发布游记并参与社区互动，以便分享旅程并建立关注关系 | 内容应经过审核；点赞、收藏、评论和关注应具备权限校验与幂等性 | UC09 |
| REQ-10 | 作为管理员，我希望管理资源、用户和订单，以便维持平台业务正常运行 | 管理功能应受 RBAC 保护，关键操作应保留审计日志 | UC10 |
| REQ-11 | 作为管理员，我希望处理内容与举报并查看运行指标，以便保障内容安全和系统可观测 | 审核、举报和敏感词变更应可追踪；日志和指标查询失败不得阻断审核 | UC11 |
| REQ-12 | 作为验收人员，我希望每个业务场景都有完整证据，以便复核系统是否真正完成 | UC01—UC11 均应具备需求、系统级图、组件级图、对象级图、代码、测试和执行报告 | UC01—UC11 |

#### 3.4.2 系统总用例图

- **图片编号**：REQ-FIG-01
- **图名**：TravelMate 完整系统用例图

**画法：**

1. 用一个大矩形表示 `TravelMate 系统边界`，矩形外放置主要参与者：游客、已登录用户、管理员、外部 AI 服务。
2. 系统边界内按五个业务域分组放置 UC01—UC11，不把查询页面、按钮、Controller、优惠券核销或数据库操作画成独立用例。
3. 游客关联 UC01、UC04；用户关联 UC01—UC09 中适用场景；管理员关联 UC05、UC09、UC10、UC11；外部 AI 服务关联 UC06、UC07。
4. 可使用 `<<include>>` 表示“预订包含身份校验、乘客/游客选择、库存与优惠校验”，使用 `<<extend>>` 表示“无票时提交候补”“AI 失败时降级”“履约后评价/举报”等条件扩展。
5. 图中必须显示 UC 编号和完整业务目标，例如 `UC02 大交通查询、预订与订单履约`，不能只写“航班查询”或“支付”。


#### 3.4.3 完整用例说明

每个用例均以参与者触发并产生可验证业务结果为边界。主成功流程、备选流程和异常流程属于同一用例的不同路径，不另拆成按钮级或接口级用例。

| 用例 | 参与者、触发条件与前置条件 | 主成功流程 | 备选或异常流程 | 可验证结果 | 系统级模型 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| UC01 账户与个人资料安全管理 | 游客/用户注册、登录、改密或维护主页；账号有效，受保护操作时已登录 | 注册/登录 → 获取 JWT → 查看或修改资料/密码 → 返回处理结果 | 用户名重复、密码错误、Token 失效、账号禁用或越权时拒绝 | 用户和资料记录正确；有效用户可访问，非法请求返回 401/403 | SYS-UC01 / REQ-FIG-03 |
| UC02 大交通查询、预订与订单履约 | 用户计划乘坐航班或火车；已登录并选择有效乘客 | 查询班次 → 选择舱位/席别、乘客和优惠券 → 有票建单/无票候补 → 模拟支付 → 查看凭证 → 可取消或退款 | 无班次、库存不足、无票候补、优惠无效、超时、重复操作或非法状态时进入对应分支 | 订单或候补记录可查；金额、旅客、优惠、状态和库存一致 | SYS-UC02 / REQ-FIG-04 |
| UC03 酒店查询、订房与订单处理 | 用户输入城市和日期；已登录且目标房型可售 | 搜索酒店 → 选择房型和入住信息 → 使用可用优惠 → 扣减库存并建单 → 支付 → 可取消或退款 | 日期非法、库存不足、缓存故障、优惠无效、超时或重复退款时进入对应分支 | 酒店订单可查；金额与入住信息正确；取消/退款后库存正确回补 | SYS-UC03 / REQ-FIG-05 |
| UC04 景点及本地游产品预订 | 用户选择景点票或一日游/周边游产品；提交订单时已登录 | 查看产品 → 选择票种/日期/人数和联系人 → 校验可售状态 → 使用可用优惠 → 创建订单并返回凭证 | 产品下架、票量不足、日期不可售、游客无效、未登录或重复提交时拒绝 | 成功时订单和凭证可查；失败时不扣库存、不产生脏订单 | SYS-UC04 / REQ-FIG-06 |
| UC05 履约后评价、回复与举报处理 | 用户对已完成订单评价；商家/管理员回复或处理举报 | 校验履约资格 → 保存评分图文 → 回复 → 用户举报 → 生成工单 → 管理员处理 | 重复评价、非法评分、空内容、越权回复或重复举报时拒绝 | 评价、回复、举报工单和处理终态关联正确 | SYS-UC05 / REQ-FIG-07 |
| UC06 AI 行程生成、匹配与保存 | 用户提交目的地、日期、预算和偏好；已登录 | 校验参数 → 调用 AI → 校验 JSON → 匹配站内资源 → 保存并展示行程 | API Key 缺失、超时、JSON 非法或地点无法匹配时使用模板或部分匹配 | 行程记录可查询；正常与降级响应均满足前端结构 | SYS-UC06 / REQ-FIG-08 |
| UC07 AI 客服多轮对话 | 用户在同一会话连续提问；已登录且消息非空 | 读取上下文 → 调用 AI → 保存问答 → 返回回答 → 继续下一轮 | AI 超时或失败时返回明确提示，不编造实时库存和订单信息 | 多轮消息按会话保存，上下文连续，失败结果可解释 | SYS-UC07 / REQ-FIG-09 |
| UC08 通知中心与站内私信 | 业务事件产生通知，或用户主动发私信；用户已登录 | 写入/查看通知 → 标记已读或删除；选择联系人 → 发送并查询会话 | 接收者不存在、空消息或越权读取时拒绝；通知失败不回滚核心交易 | 未读数、已读状态和会话消息正确，无越权泄露 | SYS-UC08 / REQ-FIG-10 |
| UC09 游记发布审核与社区互动 | 用户发布游记或参与互动；管理员处理待审内容 | 上传图文 → 敏感词检测 → 审核发布/驳回 → 用户点赞、收藏、评论、关注或取消操作 | 上传失败、命中敏感词、越权编辑/删除、非法评论、自关注或重复操作时拒绝/幂等处理 | 内容审核状态、互动关系、树状评论和关注计数一致 | SYS-UC09 / REQ-FIG-11 |
| UC10 管理后台业务运营 | 管理员维护资源、订单和用户；已通过 RBAC 校验 | 查询并维护资源 → 启停用户 → 查看订单 → 审核退款 → 写入审计日志 | 普通用户返回 403；非法导入行提示；被引用资源不得物理误删 | 业务数据状态正确，关键管理动作和操作人可追踪 | SYS-UC10 / REQ-FIG-12 |
| UC11 内容安全与系统可观测 | 管理员进入审核、举报、敏感词或监控页面；具有对应权限 | 处理内容/举报 → 维护敏感词 → 查看日志、QPS 和业务指标 | 重复处理保持终态；统计失败不阻断审核；无权限返回 403 | 内容、工单和词库状态正确，日志与指标可查询 | SYS-UC11 / REQ-FIG-13 |

> 范围说明：原清单中的班次查询、常用旅客、优惠券、支付退款、点赞收藏和关注等内容属于完整业务场景中的步骤或异常路径，已合并到对应端到端用例，不再单独占用系统级配图。当前本地游产品预订闭环和优惠券订单核销仍需补齐，在完成前不得标记为已验收。

#### 3.4.4 概念类图

- **图片编号**：REQ-FIG-02
- **图名**：TravelMate 领域概念类图

**画法：**

1. 只画业务概念、关键属性和业务关系，不画 Controller、Service、Mapper、DTO、接口路径或数据库类型。
2. 核心概念包括：`User`、`Passenger`、`Flight`、`Train`、`TrafficOrder`、`Hotel`、`RoomType`、`HotelOrder`、`Attraction`、`AttractionOrder`、`TourProduct`、`TourOrder`、`Coupon`、`UserCoupon`、`Review`、`ReviewReport`、`AiPlan`、`AiChatSession`、`Notification`、`PrivateMessage`、`Post`、`Comment`、`Like`、`Favorite`、`Follow`、`AdminAudit`、`SysLog`。
3. 标出基数：用户 `1..*` 拥有旅客、订单、行程和帖子；酒店 `1..*` 包含房型；帖子 `1..*` 包含评论；优惠券与用户通过 `UserCoupon` 形成多对多关系。
4. 交通订单可关联航班或车次，使用业务上的可选关联 `0..1`，避免把航班和火车错误画成同一个实体。
5. 标出关键状态属性：订单状态、游记审核状态、举报处理状态、优惠券使用状态；概念图不展开状态迁移细节。


#### 3.4.5 UC01—UC11 系统级模型清单

系统级模型把 TravelMate 视为黑盒，只表现参与者发出的系统事件、系统响应、外部系统交互和业务状态，不画内部代码对象。图片统一使用白底、深色文字；成功流程用蓝/绿色，异常或拒绝分支用橙/红色；每张图标题必须同时包含 `SYS-UCxx` 和用例名称。

| 图片编号 | 模型编号 | 图名 | 类型 | 画法 |
| :--- | :--- | :--- | :--- | :--- |
| REQ-FIG-03 | SYS-UC01 | 账户与个人资料安全管理活动图 | 活动图 | 从注册/登录分支开始，汇合到身份认证；登录后可查看或修改资料/密码；异常分支包含用户名重复、密码错误、Token 失效、账号禁用和越权 |
| REQ-FIG-04 | SYS-UC02 | 大交通预订与履约活动图 | 活动图 | 使用用户、TravelMate、管理员三条泳道；画查询航班/车次、选择乘客与优惠、有票建单/无票候补、支付、凭证、取消/退款和库存回补；异常包含库存不足、超时及重复操作 |
| REQ-FIG-05 | SYS-UC03 | 酒店订房与订单处理活动图 | 活动图 | 画搜索酒店、选择房型、日期/库存/优惠校验、创建订单、支付、取消/退款和库存回补；分支包含缓存降级、库存不足、日期非法和重复退款 |
| REQ-FIG-06 | SYS-UC04 | 景点及本地游产品预订活动图 | 活动图 | 从选择景点票或本地游产品分支开始，汇合到日期/人数/游客/优惠校验和建单；异常包含产品下架、票量不足、日期不可售、未登录及重复提交 |
| REQ-FIG-07 | SYS-UC05 | 履约后评价与举报处理活动图 | 活动图 | 使用用户、TravelMate、管理员/商家三条泳道；画履约资格校验、保存评价、回复、举报、生成工单和处理终态；异常包含重复评价、非法评分、越权回复和重复举报 |
| REQ-FIG-08 | SYS-UC06 | AI 行程生成与降级系统级顺序图 | 系统级顺序图 | 用户、TravelMate、外部 AI 三条生命线；画提交参数、调用 AI、结构校验、资源匹配、保存和返回；使用备选片段画配置缺失、超时、非法 JSON、部分匹配和模板降级 |
| REQ-FIG-09 | SYS-UC07 | AI 客服多轮对话系统级顺序图 | 系统级顺序图 | 用户、TravelMate、外部 AI 三条生命线；至少画两轮提问，第二轮携带上下文；使用备选片段画 AI 超时/失败并返回可解释提示 |
| REQ-FIG-10 | SYS-UC08 | 通知中心与站内私信活动图 | 活动图 | 起点分成业务通知和主动私信两条路径；画通知写入/查询/已读/删除，以及联系人选择/发送/接收/查询会话；增加通知失败不回滚交易和越权拒绝 |
| REQ-FIG-11 | SYS-UC09 | 游记审核与社区互动活动图 | 活动图 | 使用用户、TravelMate、管理员三条泳道；画图文上传、敏感词检测、审核发布/驳回，再连接点赞、收藏、评论和关注；异常包含上传失败、越权操作、非法评论和自关注 |
| REQ-FIG-12 | SYS-UC10 | 管理后台业务运营活动图 | 活动图 | 管理员通过 RBAC 后分为资源维护、用户启停和订单/退款审核三条支路，最终汇合到审计日志；异常画普通用户 403、非法导入和被引用资源处理 |
| REQ-FIG-13 | SYS-UC11 | 内容安全与系统可观测活动图 | 活动图 | 管理员通过权限校验后分为内容审核、举报处理、敏感词维护、日志指标查询四条路径；画重复处理幂等、统计失败不阻断审核和词库更新失败保留旧规则 |

下表和 3.4.6 节仅提供画图描述。绘图时应保留主成功流程、关键异常分支和最终可验证结果。

#### 3.4.6 配图要点

以下内容仅保留每张需求模型图的画图要点。绘图时应使用对应图片编号和图名，并完整表达主成功流程、异常分支与可验证结果。

##### REQ-FIG-01 TravelMate 完整系统用例图

**画图要点：**

- 系统边界外只放游客、用户、管理员和外部 AI 服务四类参与者。
- 系统边界内放 UC01—UC11，查询、支付、优惠核销、点赞等步骤不单列用例。
- 预订类用例共同依赖账户身份；AI 行程和 AI 客服与外部 AI 服务关联。



##### REQ-FIG-02 TravelMate 领域概念类图

**画图要点：**

- 只画用户、资源、订单、权益、AI、消息、社区和管理审计等业务概念。
- 标出用户与订单/内容的一对多、酒店与房型的一对多、用户与优惠券的多对多等基数。
- 不出现 Controller、Service、Mapper、DTO 和接口路径。



##### REQ-FIG-03 SYS-UC01 账户与个人资料安全管理活动图

**画图要点：**

- 注册和登录是两个入口，成功后汇合到已认证状态。
- 已认证用户可以维护资料或修改密码。
- 用户名重复、密码错误、Token 失效、账号禁用和越权均进入拒绝结果。



##### REQ-FIG-04 SYS-UC02 大交通预订与履约活动图

**画图要点：**

- 一张图覆盖航班/火车查询、旅客和优惠选择、建单/候补、支付、凭证、取消退款。
- 有票和无票形成建单与候补分支；支付后再按用户选择进入履约或退款路径。
- 库存不足、优惠无效、超时和重复操作要有明确终点。



##### REQ-FIG-05 SYS-UC03 酒店订房与订单处理活动图

**画图要点：**

- 从搜索酒店开始，覆盖房型、日期、入住人、优惠、库存和建单。
- 支付、超时关闭、取消退款和库存回补在同一闭环中表达。
- Redis 不可用时应画出数据库校验降级路径。



##### REQ-FIG-06 SYS-UC04 景点及本地游产品预订活动图

**画图要点：**

- 景点票和本地游产品作为两个入口，之后汇合到预订校验和建单。
- 校验票种/日期、人数、游客/联系人、优惠和库存。
- 产品下架、日期不可售、未登录、库存不足和重复提交分别返回失败结果。



##### REQ-FIG-07 SYS-UC05 履约后评价与举报处理活动图

**画图要点：**

- 使用用户、TravelMate、管理员/商家三类责任角色表达完整处理链。
- 先校验履约和评价资格，再保存评价、回复、举报、生成工单和处理终态。
- 重复评价、非法评分、越权回复和重复举报都应有拒绝路径。



##### REQ-FIG-08 SYS-UC06 AI 行程生成与降级系统级顺序图

**画图要点：**

- 只画用户、TravelMate 和外部 AI 服务三条生命线。
- 正常路径包含参数提交、AI 调用、JSON 校验、资源匹配、保存和返回。
- 配置缺失、超时、非法 JSON 和部分地点无法匹配使用 `alt`/`else` 表达。



##### REQ-FIG-09 SYS-UC07 AI 客服多轮对话系统级顺序图

**画图要点：**

- 至少展示两轮提问，第二轮明确携带第一轮上下文。
- 用户消息和 AI 回答均由 TravelMate 保存到同一会话。
- 外部 AI 超时或失败时返回可解释提示，不能返回伪造实时业务数据。



##### REQ-FIG-10 SYS-UC08 通知中心与站内私信活动图

**画图要点：**

- 从“业务事件通知”和“用户主动私信”两个入口展开。
- 通知路径覆盖写入、查询、标记已读和删除；私信路径覆盖联系人、发送和会话查询。
- 通知失败不能回滚核心交易，越权访问和空消息必须拒绝。



##### REQ-FIG-11 SYS-UC09 游记审核与社区互动活动图

**画图要点：**

- 使用用户、TravelMate、管理员三类责任角色表达发布审核闭环。
- 审核通过后才能进入点赞、收藏、评论和关注互动。
- 上传失败、敏感词、越权编辑删除、非法评论、自关注和重复互动均需处理。



##### REQ-FIG-12 SYS-UC10 管理后台业务运营活动图

**画图要点：**

- 所有管理操作先经过 RBAC 校验。
- 业务分支包括资源维护、用户启停和订单/退款审核，最终都写审计日志。
- 普通用户、非法导入和被引用资源删除分别给出失败结果。



##### REQ-FIG-13 SYS-UC11 内容安全与系统可观测活动图

**画图要点：**

- 权限校验后分成内容审核、举报处理、敏感词维护和日志指标查询四条路径。
- 审核与举报操作必须幂等并保留终态；敏感词更新失败保留旧规则。
- 指标查询失败只能影响监控展示，不能阻断内容审核。



#### 3.4.7 AI 行程生成与调度
输入：目的地、天数、预算、人数、偏好标签。
处理：构造Prompt异步调用DeepSeek API，通过JSON Output约束返回结果，并匹配站内Mock资源。
输出：分天展示的JSON行程数据及推荐资源列表。
#### 3.4.8 订单并发与库存控制
行为：用户下单时，Redis优先预减库存，数据库使用原子语句校验，防止超卖。
时限：超时15分钟未支付订单需自动触发库存回滚。
#### 3.4.9 内容审核与社交安全
功能：对UGC内容进行敏感词自动过滤与人工复核，确保社区合规性。

### 3.5 CSCI外部接口需求
#### 3.5.1 接口标识和接口图
系统与DeepSeek API、第三方地图API及文件存储服务进行交互。
#### 3.5.2 用户接口
风格：卡片式设计，蓝色系主调。组件：Element Plus（统一用于 PC Web 端）。
#### 3.5.3 硬件接口
运行于标准个人计算机，适配1080P及以上分辨率显示器。
#### 3.5.4 软件接口
DeepSeek API：采用OpenAI兼容协议，Bearer Auth认证。
支付模拟：内部Webhook回调，不接真实网关。
#### 3.5.5 通信接口
协议：HTTP/HTTPS,RESTful API。
数据格式：标准 JSON。

### 3.6 CSCI内部接口需求
子系统C(AI)需调用子系统A/B提供的资源检索接口获取Mock数据。
子系统D生成的内容需推送到子系统E的审核流接口。

### 3.7 ==CSCI内部数据需求==
核心业务概念、关键属性、关系和基数以 3.4.4 节 REQ-FIG-02《TravelMate 领域概念类图》为需求基线。概要设计和详细设计可以增加技术类与实现关系，但不得改变本需求说明中已经确认的业务概念含义。

### 3.8 适应性需求
前端需通过Vue路由懒加载和Vite资源分包适应不同网络环境下的加载速度。

### 3.9 保密性需求
身份验证：基于JWT的Token验证机制。
权限控制：基于RBAC的角色访问控制，防止越权访问。

### 3.10 保密性和私密性需求
用户密码存储需进行哈希加密，社区隐私字段在接口层进行数据脱敏。

### 3.11 ==CSCI环境需求==
开发工具：JDK 21,Node.js,Git,IDE。
部署工具：Nginx配置HTTP Gzip压缩。

### 3.12 计算机资源需求
#### 3.12.1 计算机硬件需求
本系统(CSCI)主要部署于云端服务器或本地模拟环境，具体硬件需求如下：
处理器(CPU)：建议2核及以上，以支持Java虚拟机的并发处理与大模型API的异步调度。
存储器(RAM)：系统运行总内存需求不低于4GB。其中，Java运行时环境(JRE)建议分配1GB-2GB堆内存，MySQL与Redis实例各需约512MB运行空间。
辅助存储器(硬盘)：初始占用空间约2GB(含系统镜像与基础数据)，建议预留20GB以上固态存储(SSD)空间，用于存储UGC社区图片、系统运行日志及数据库持久化文件。
通信/网络设备：需配备标准的以太网卡或无线网络适配器，支持持续的互联网接入，以确保与外部DeepSeek API及其它第三方地图服务联通。
#### 3.12.2 计算机硬件资源利用需求
为保证系统在演示及测试阶段的稳定性，硬件资源利用限制如下：
处理器利用率：在正常负载下(如单用户行程规划)，平均CPU利用率应低于30%；在模拟并发下单场景下，峰值CPU利用率不应长时间超过70%。
存储器容量利用：系统常驻内存占用应控制在分配总额的80%以内。Redis缓存需配置过期策略，防止Mock数据堆积导致内存溢出。
辅助存储器利用：由于采用图片压缩技术，UGC存储空间增长应可控；系统需配置日志滚动删除策略，确保磁盘占用率不超过90%。
网络带宽利用：单次AI交互产生的数据流量通常在KB级别，峰值带宽占用主要集中在前端静态资源初次加载与社区图片上传阶段。
#### 3.12.3 计算机软件需求
系统开发与运行必须依赖以下软件环境：
操作系统：服务端建议使用Linux(Ubuntu 20.04 LTS 或 CentOS 7.9 及以上)；客户端支持Windows 10/11、macOS或现代移动端浏览器。
数据库管理系统：MySQL 8.0.x，用于核心业务数据的持久化存储。
缓存软件：Redis 6.2或7.x版本，用于库存预占、会话管理及热点Mock数据缓存。
运行环境与实用软件：
Java Development Kit (JDK) 21：后端服务运行基石。
Nginx 1.20+：负责静态资源托管及后端API的反向代理。
Node.js 18+：前端构建工具Vite的运行环境。
外部API引用：DeepSeek API(兼容 OpenAI 协议版本)，用于支持AI智能规划的核心功能。
#### 3.12.4 计算机通信需求
系统涉及的通信机制与性能指标要求如下：
配置与拓扑结构：采用典型的前后端分离架构。前端(Browser)通过公共网络与后端(Spring Boot Server)连接，后端在内网环境下与MySQL和Redis实例通信。
传输技术与协议：全站基于TCP/IP协议，应用层统一采用HTTP/HTTPS协议及RESTful API规范。
数据传输速率：后端接口响应(不含 AI 生成)时间应在500ms以内；AI行程生成受限于大模型算力，首字响应(Stream)或整体返回时间应控制在3-10秒。
网关需求：需支持跨域资源共享(CORS)配置，以允许前端单页面应用(SPA)的安全访问。
数据的峰值与容量：系统需具备处理短时间内每秒数十次(QPS)请求的能力，特别是针对订票与订房的库存减扣接口。

### 3.13 ==软件质量因素==
#### 3.13.2 可靠性
数据一致性：在高并发模拟场景下，订票与订房库存不得出现超卖现象，数据库与Redis缓存状态需最终一致。
服务可用性：系统应具备处理大模型API超时或宕机的异常处理机制，保证在非核心功能失效时主流程依然可访问。
#### 3.13.3 可维护性
规范化：代码需遵循阿里巴巴Java开发手册规范，前端遵循Vue 3组合式API开发规范。
模块化：采用前后端分离架构，各功能模块通过RESTful API通信，确保单个模块的修改不影响整体系统。
#### 3.13.4 易用性
交互效率：用户从输入行程需求到生成计划的操作步骤应控制在3步以内。
反馈及时性：所有写操作(如提交订单)需在1秒内给出反馈。AI生成等长耗时操作需提供Loading状态或流式输出。

### 3.14 设计和实现的约束
技术栈约束：后端必须使用JDK 21与Spring Boot 3.x；前端使用Vue 3 (Vite + JavaScript)；数据库使用MySQL 8.0与Redis。
接口规范：AI模块必须兼容OpenAI API协议，采用异步调用机制以防止阻塞主线程。
数据标准：所有地理位置信息需符合标准经纬度格式，订单金额统一使用分(Integer 类型)存储。
安全性约束：用户密码必须经过BCrypt或类似的单向哈希加密存储，敏感接口需携带JWT令牌验证。

### 3.15 数据
输入数据：用户注册信息、行程偏好(地点、预算、天数等)、订单支付指令、UGC社区图文内容。
输出数据：AI生成的结构化行程JSON、订单状态凭证、可视化系统监控报表、社区动态瀑布流。
处理量与数据量：系统应支持至少100个模拟并发连接；数据库设计需支持万级订单量与十万级Mock资源数据的快速检索。

### 3.16 操作
常规操作：用户通过浏览器访问平台，进行登录、搜索资源、AI规划行程、下单及社区发布。
初始化操作：通过执行init.sql脚本完成数据库表结构与基础Mock数据的装载；通过Nginx配置文件完成前端静态资源与后端API反向代理的映射。
恢复操作：若系统宕机，需支持通过重启JAR包及Redis实例实现快速服务恢复；数据库需支持基于Binlog的增量恢复。

### 3.17 故障处理
API 故障：当DeepSeek API响应超时或由于额度不足返回错误时，系统需拦截错误并返回预定义的“推荐行程模版”，确保用户不看到空白页面。
库存冲突：当多用户同时抢购最后一份资源时，系统应捕获数据库OptimisticLockingFailureException，并向用户提示“库存已售罄”。
错误信息定义：
ERR_001: AI服务暂不可用，请稍后再试。
ERR_002: 库存不足，订单关闭。
ERR_003: 登录过期，请重新认证。

### 3.18 算法说明
#### 3.18.1 AI 结构化生成算法
概况：利用Prompt Engineering将非结构化的用户需求转化为结构化的JSON数据。
实现细节：系统构建一个包含出行背景的System Prompt，要求模型按预定义的分天行程Schema输出（包含：活动名称、时间点、地点、费用估算）；AI响应落库后支持历史记录查询；API超时时自动降级返回预设模板行程。
#### 3.18.2 库存防超卖算法
概况：基于Redis原子操作与Lua脚本的分布式限流与扣减算法。
公式：IF (Redis.get(stock_key) >= req_num) THEN Redis.decrby(stock_key, req_num) ELSE RETURN -1。

### 3.19 有关人员需求
普通用户：需具备基础的Web浏览器操作能力，无需专业背景。
系统管理员(开发者)：需熟悉Linux基础命令、Git版本控制以及Spring Boot监控端点(Actuator)的使用。
并发需求：系统需支持50-100名用户同时在线进行行程查询。

### 3.20 有关培训需求
系统应交付《用户操作指南》，涵盖如何启动AI行程助手以及如何进行订单核销。
开发者需提供《部署维护文档》，说明环境搭建、API Key配置及数据库迁移步骤。

### 3.21 有关后勤需求
维护支持：采用GitHub/GitLab进行代码版本管理。
托管环境：可运行于配备4G内存以上的轻量级云服务器或本地实验环境计算机。

### 3.22 其他需求
可观测性：系统管理端需集成轻量级监控大屏，实时展示系统QPS、AI响应耗时及错误日志占比。

### 3.23 包装需求
交付物组成：源代码压缩包、数据库初始化.sql文件、README.md(包含运行步骤)、演示视频(MP4 格式)。
标签规范：代码库需打上v1.0-final的Release标签。

### 3.24 需求的优先次序和关键程度
关键(Essential)：用户认证、AI行程生成主流程、订单支付逻辑、库存安全校验。
重要(Important)：社区图文上传、后台资源管理模块、系统监控报警。
重要(Important)：历史价格趋势 ECharts 图表展示、CSV 批量数据导入、候补抢票系统。
可选(Optional)：多语言切换、AI语音输入交互。

## 4 合格性规定

| 验收项 | 合格标准 | 合格性方法 |
| :--- | :--- | :--- |
| 用例完整性 | 经教师/助教确认的 UC01—UC11 均可运行，并产生本说明书规定的可验证结果 | 按用例逐项演示，检查数据库、接口响应、页面结果和异常路径 |
| 需求与模型 | 每个 UC 均有 REQ、用例说明、SYS、COMP、OBJ 编号，总用例图和概念类图内容一致 | 文档审查和编号交叉检查 |
| 测试与追溯 | 每个 UC 均有 UNIT、INT、E2E 测试及实际报告、截图、执行时间和 commit | 检查测试代码、原始报告和追溯矩阵，不接受只写“通过” |
| 并发性能 | 并发下单时库存不超卖、不出现负数，订单与库存最终一致 | 使用 JMeter 进行 50 线程以上压力测试并核对数据与日志 |
| AI 降级 | AI 超时、配置缺失或结构非法时返回可解释的降级结果，主页面不空白 | 故障注入、接口断言和页面演示 |
| 接口规范 | 接口采用 HTTP/HTTPS、RESTful、JSON 和统一错误响应 | 检查 Knife4j/接口文档、请求响应和实现代码 |
| 安全性 | 密码哈希、JWT 鉴权、RBAC 和隐私字段裁剪有效 | 代码审查、数据库抽检和未认证/越权测试 |

## 5 需求可追踪性

本说明书中的需求采用以下追溯链：

`REQ-xx → UCxx → SYS-UCxx → COMP-UCxx → OBJ-UCxx → 主要代码 → UNIT-TC-1xx / INT-TC-1xx / E2E-TC-1xx → 实际报告与 commit`

1. REQ-01—REQ-12 与 UC01—UC11 的对应关系见 3.4.1 节。
2. UC01—UC11 的系统级模型为 SYS-UC01—SYS-UC11，对应 REQ-FIG-03—REQ-FIG-13。
3. 组件级模型 `COMP-UCxx` 应在概要设计说明书中保持同一用例边界；对象级模型 `OBJ-UCxx` 应在详细设计说明书中落实到真实代码对象。
4. 测试编号必须与 UC 后两位保持对应，例如 UC02 使用 UNIT-TC-102、INT-TC-102、E2E-TC-102。
5. 只有设计、代码、测试和实际运行证据全部齐全时，用例才能标记为“通过”；代码可定位不能代替运行验收。
6. 教师或助教确认后的用例清单是最终验收范围；范围变更必须记录日期、确认人、原因和受影响的需求、图、代码与测试。

## 6 ==尚未解决的问题==
外部API稳定性：目前依赖的第三方DeepSeek API存在突发性响应延迟，未来考虑引入本地模型或缓存池作为备选。
支付接口集成：当前为模拟支付，若需上线需重新对接真实支付网关(如支付宝、微信支付)。

## 7 注解
CSCI: Computer Software Configuration Item，本说明书指代“伴游”系统整体。
UGC: User Generated Content，用户生成的社区内容。
JWT: JSON Web Token，用于身份认证的开放标准。

## 附录
无
