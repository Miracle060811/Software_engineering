# TravelMate 微服务验收差距整改测试报告（2026-09-01—2026-09-02）

## 1. 结论

本轮已完成并合入 `main`：六服务 API 契约覆盖、UC01—UC19 真实微服务 E2E 门禁、单服务定向部署/回滚脚本及回归测试，以及前端部署镜像 `libexpat` 高危漏洞修复。对应 PR #224、#225、#226、#228 均已合并。

最终发布基线为 commit `4850f4805db158522bdd5a952b0be4efb827cf66`。GitHub Actions [run 33582799133](https://github.com/Miracle060811/Software_engineering/actions/runs/33582799133) 第 2 次尝试为 `success`：代码质量门禁、两套真实 E2E、镜像安全扫描、八个应用镜像发布、Docker Desktop Kubernetes 部署及最终 delivery gate 全部通过。

仍不能宣称所有运行实验完成：单服务更新/失败回滚目前只有 fake `kubectl` 回归，尚未在真实集群执行故障注入；HPA 与 k6 负载实证也未执行。

## 2. 整改项与证据

| 整改项 | 结果 | 精确证据 | 边界 |
| --- | --- | --- | --- |
| T1 六服务 API 契约覆盖 | 已合入并通过 | PR [#224](https://github.com/Miracle060811/Software_engineering/pull/224)，merge commit `966c053509d7ba51519d532fb4b4ef4486d988c5`；覆盖 113 个端点，其中 94 个公开端点、19 个内部端点；本地六服务 Maven 共 100 项测试无失败；最终 main 流水线六服务测试与 API coverage gate 全绿 | 端点覆盖证明契约测试登记与执行，不等价于每个业务分支均有端到端场景 |
| T2 六服务真实 E2E 门禁 | 已合入并通过 | PR [#225](https://github.com/Miracle060811/Software_engineering/pull/225)，merge commit `0d93f2e3f25e1c622f644af38fb77eaf50c77da1`；最终 main 流水线 `Real microservice E2E` 为 17/17 通过，覆盖 UC01—UC19；六服务独立 JAR、六个 Schema、真实 MySQL/Redis 均参与运行 | 17 个场景联合覆盖 19 个 UC，并非 19 个独立测试 |
| T2 单体回归 E2E | 通过 | 最终 main 流水线 `Real backend E2E` 为 17/17 通过；使用打包后端、隔离 MySQL，并验证 Flyway 初始化与管理员 bootstrap | 这是单体回归证据，与六服务 E2E 分开统计 |
| T2 失败修复 | 通过 | 首次 run `33579497229` 为 16/17；UC01 浏览器登录返回 `403 Invalid CORS request`。修复 CI 的 `CORS_ALLOWED_ORIGINS=http://127.0.0.1:3100`，并将配置纳入结构回归；后续为 17/17 | 只放宽 CI 专用 Origin，不改变默认业务环境白名单 |
| T4 单服务独立更新/回滚 | 代码与回归通过 | PR [#226](https://github.com/Miracle060811/Software_engineering/pull/226)，merge commit `20632fdad7b27b9a224c6e2e4ac0db460f11996f`；`-Service` 只修改目标 Deployment 的 image、annotation、rollout 与 rollback；回归验证其他五服务不变 | 尚未在真实 Kubernetes 集群执行目标服务更新和失败回滚 |
| 部署镜像安全修复 | 已合入并通过 | run `33581387956` 的 frontend Trivy 扫描发现 `CVE-2026-66046`、`CVE-2026-76641`，`libexpat 2.8.2-r0` 需升级至 `2.8.4-r0`；PR [#228](https://github.com/Miracle060811/Software_engineering/pull/228)，merge commit `4850f4805db158522bdd5a952b0be4efb827cf66`；最终 backend/frontend Trivy 扫描通过 | 未放宽安全门禁；首次流水线按设计阻止了部署 |

本轮结构门禁结果：

- `npm run check:microservice-e2e`：UC01—UC19 结构覆盖 19/19，runnable 19，blocked 0。
- `npm run check:traceability`：完整覆盖 19、部分覆盖 0、尚待测试 0、结构错误 0，自动化证据分 38/38。
- `npm run check:api-coverage`：六服务 API 契约清单与测试锚点校验通过。
- `npm run check:deployment`：数据库迁移、Kubernetes manifests、PowerShell 语法与定向部署/回滚回归通过。

## 3. 最终 main 发布与 Kubernetes 证据

### 3.1 不可变镜像

| 组件 | 部署镜像摘要 |
| --- | --- |
| backend | `ghcr.io/miracle060811/travelmate-backend@sha256:01abdbc4d1223a468c2c45113135186d7344d63ca27cbeb5014cbe2a148d3097` |
| frontend | `ghcr.io/miracle060811/travelmate-frontend@sha256:0c51849e322021b9d461dcdcec23d846f165ecb5c799aefd511f73b0f8967acc` |
| identity-service | `ghcr.io/miracle060811/travelmate-identity-service@sha256:df7848ec94c720b0ce1adf0fa4f45dee4d59adf2057597b0775c11ecfaf1ab80` |
| traffic-service | `ghcr.io/miracle060811/travelmate-traffic-service@sha256:3de689301bb59af081df98ac969c09e361985df41066bb0e39d69bef6aeffb2b` |
| local-service | `ghcr.io/miracle060811/travelmate-local-service@sha256:e83251154c18e99c22af038b1578b0990a1dd3ce3d338a708b1e2133d28e6180` |
| ai-service | `ghcr.io/miracle060811/travelmate-ai-service@sha256:c504466610029f2431e8368f7bc9b99f4fc988aaba8248fae8a07907a377a306` |
| community-service | `ghcr.io/miracle060811/travelmate-community-service@sha256:db458ac56becb0817d898be3cf99082d14a034d3af5def4a823581a2156254d3` |
| ops-service | `ghcr.io/miracle060811/travelmate-ops-service@sha256:1a18f969e418610566721edcb70f21d6dca29d83117ac051e55af3ceb23e88bf` |

发布证据 `deployment-release-4850f4805db158522bdd5a952b0be4efb827cf66` 与 `microservice-release-4850f4805db158522bdd5a952b0be4efb827cf66` 均记录 commit、source run ID 和 digest；部署阶段按 digest 核对实际镜像。

### 3.2 集群状态

最终 artifact `kubernetes-deployment-4850f4805db158522bdd5a952b0be4efb827cf66`（attempt 2，artifact ID `9829454508`）显示：

- 六个微服务 Pod 均为 `1/1 Running`，微服务容器重启数合计为 0。
- backend 2 个 Pod、frontend 2 个 Pod 均为 `1/1 Running`。
- 8081—8086 六个微服务健康端点、backend 与 frontend 健康端点均返回 HTTP 200。
- MySQL、MinIO 与 uploads 三个 PVC 均为 `Bound`。
- 部署镜像与发布 evidence 中的 8 个不可变 digest 一致。

### 3.3 失败与重试记录

最终 run 第 1 次尝试失败在自托管 runner 的 `Checkout released commit over HTTPS`：步骤 5 分钟超时，下载 artifact 也出现 100 秒网络超时；delivery gate 正确失败。仅重跑失败作业后，第 2 次尝试完成 checkout、发布证据下载、部署、digest 核对和健康检查，最终 delivery gate 通过。没有通过跳过或放宽门禁掩盖失败。

## 4. 当前未完成项

| 未完成项 | 完成条件 |
| --- | --- |
| 单服务真实更新 | 在可用 `kubectl` context 上只更新一个服务，保存更新前后六服务 imageID、Pod UID、rollout status 和健康结果 |
| 单服务真实回滚 | 经明确授权后注入一个不可用目标镜像，证明仅目标服务失败并回滚到原 digest，其他五服务 Pod UID/imageID 不变 |
| HPA/性能实证 | 准备 Metrics Server 与 k6，经明确授权执行负载，保存 HPA、Pod、CPU 和 k6 输出；静态 HPA YAML 不算运行实证 |

## 5. 复验命令

```powershell
npm run check:traceability
npm run check:microservice-e2e
npm run check:deployment
npm run check:api-coverage
kubectl config current-context
```

真实集群演练应使用 `scripts/cd/Deploy-TravelMateMicroservices.ps1 -Service <service>`，并将输出写入独立 evidence 目录；不得用 fake `kubectl` 结果替代真实集群证据。
