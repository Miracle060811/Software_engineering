# TravelMate 伴游平台 详细实施计划

> 基于《5组-软件详细设计说明.md》与《5组-软件需求规格说明.md》制定
> 制定日期：2026-05-11
> 最近核对：2026-05-12（按当前仓库代码实现修订）

---

## 一、项目现状分析

### 已完成

| 模块           | 完成内容                                                                                                                                                           |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 用户认证       | JWT 登录/注册、Spring Security、JwtFilter、前端路由鉴权                                                                                                            |
| 大交通票务     | Flight/Train 搜索与 CRUD、TrafficOrder 下单/支付/取消、Passenger 管理、PriceHistory 查询                                                                           |
| 酒店与景点     | Hotel 搜索/详情/房型查询、HotelOrder 下单/支付/取消、Attraction 搜索、Review 基础评价                                                                              |
| AI 功能        | AiPlan 生成/保存/查询、AiChat 多轮对话、通知列表/已读/未读数、失败降级模板                                                                                         |
| 社区与用户中心 | Post 发布/列表/详情、Comment、Like、Follow、UserProfile、登录页与个人主页                                                                                          |
| 管理后台       | AdminController 基础统计/资源管理/游记审核/用户管理、敏感词服务、SysLog AOP、AdminDashboard 页面                                                                   |
| 前端页面       | Home、Login、FlightSearch、TrainSearch、HotelSearch、HotelDetail、AttractionList、AiPlan、Community、PostCreate、PostDetail、MyOrders、UserProfile、AdminDashboard |

### 待完善（基于 2026-05-12 代码核对）

| 子系统                            | 负责人 | 当前状态                                                     |
| --------------------------------- | ------ | ------------------------------------------------------------ |
| 大交通票务与订单                  | 邹林利 | 已完成基础闭环，待补订单详情页、状态联调和更多前端交互       |
| 目的地住宿与本地生活（酒店+景点） | 莫谨瑞 | 已完成基础链路，待补 Redis 预减库存、独立下单页和字段统一    |
| AI 智能规划与 Agent 服务          | 陈一鸿 | 已完成基础行程生成和聊天，待补真实 Skills 调用与通知中心前端 |
| 旅途社区与用户中心                | 杜新诚 | 已完成基础社区功能，待补审核流、上传增强和更多个人中心能力   |
| 管理后台与可观测性                | 李科   | 已完成基础后台接口和页面，待补监控图表、页面拆分和联调修复   |
| 前端全页面                        | 全体   | 主页面基本齐全，仍缺部分细分页面和若干前后端契约修复         |

---

## 二、技术架构

```
前端: Vue 3 + Vite + JavaScript + Element Plus + Pinia + Vue Router
后端: Java 21 + Spring Boot 3.5.x + MyBatis-Plus + Lombok
数据库: MySQL 8.0（业务数据）+ Redis（缓存/库存预减）
AI: DeepSeek API（OpenAI 兼容协议）
文件存储: 本地存储（Base64 或 MultipartFile）
认证: JWT（Bearer Token）
```

---

## 三、数据库扩展计划

当前 `docs/sql/init.sql` 已包含以下扩展表：

| 表名                  | 说明                    | 负责模块 |
| --------------------- | ----------------------- | -------- |
| `tm_hotel_room`       | 酒店房型表              | B        |
| `tm_hotel_order`      | 酒店订单表              | B        |
| `tm_attraction`       | 景点门票表              | B        |
| `tm_attraction_order` | 景点订单表              | B        |
| `tm_review`           | 评价表（酒店/景点通用） | B        |
| `tm_ai_plan`          | AI 行程规划记录表       | C        |
| `tm_ai_chat`          | AI 客服对话记录表       | C        |
| `tm_notification`     | 站内通知表              | C        |
| `tm_post`             | 社区游记表              | D        |
| `tm_comment`          | 评论表                  | D        |
| `tm_like`             | 点赞表                  | D        |
| `tm_follow`           | 关注关系表              | D        |
| `sys_log`             | 系统操作日志表          | E        |
| `sys_sensitive_word`  | 敏感词表                | E        |

---

## 四、分模块实施计划

### 模块 A：大交通票务（已有基础，完善细节）

**后端补充**：

- [x] Flight CRUD + 搜索
- [x] Train CRUD + 搜索
- [x] TrafficOrder 下单、支付模拟、取消
- [x] Passenger 常用旅客管理
- [x] PriceHistory 价格趋势
- [x] GlobalExceptionHandler（统一异常处理）
- [x] JWT / Spring Security 集成到主应用

**前端页面**：

- [x] FlightSearch（基础版）
- [ ] FlightSearch 完善（含更多筛选与预订交互）
- [x] TrainSearch（火车票搜索页）
- [x] MyOrders（订单列表页）
- [ ] TrafficOrderDetail（订单详情页）

---

### 模块 B：目的地住宿与本地生活（已完成基础开发，待联调完善）

**后端**：

- [x] `Hotel` 实体 + Controller/Service/Mapper（搜索、详情）
- [x] `HotelRoom` 实体 + Mapper（房型、余量查询已集成到 `HotelService`）
- [x] `HotelOrder` 实体 + Controller/Service/Mapper（下单、支付、取消）
  - [ ] Redis 预减库存
  - [x] 数据库原子扣减 / 乐观锁防超卖
  - [x] 超时 15 分钟未支付自动回滚（`OrderTimeoutScheduler`）
- [x] `Attraction` 实体 + Controller/Service/Mapper
- [x] `Review` 实体 + Controller/Service/Mapper（基础评价）

**前端页面**：

- [x] HotelSearch（酒店搜索页，含基础筛选）
- [x] HotelDetail（酒店详情 + 房型选择 + 下单入口）
- [ ] HotelOrder（独立酒店下单页，当前流程合并在 `HotelDetail`）
- [x] AttractionList（景点列表页）

---

### 模块 C：AI 智能规划与 Agent 服务（已完成基础开发，待补高级能力）

**后端**：

- [x] DeepSeek API 封装（`AiService`，JSON Output + 降级）
- [x] `AiPlan` 实体 + Controller/Service（行程生成、保存、查询）
  - [x] Prompt Engineering：System Prompt 约束 JSON Schema 输出
  - [x] AI 降级：API 超时时返回预设模板
- [x] `AiChat` 实体 + Controller/Service（多轮 AI 客服问答）
  - [ ] Skills 工具调用：注册查询航班/酒店为工具
- [x] `Notification` 查询/已读/未读接口
- [ ] `Notification` 主动推送/发送链路

**前端页面**：

- [x] AiPlan（AI 行程规划页：左侧输入表单，右侧结构化行程展示）
- [x] AiChat（已集成在 AiPlan 抽屉中）
- [ ] AiChat 独立页面 / 全局悬浮组件
- [ ] NotificationCenter（通知中心）

---

### 模块 D：旅途社区与用户中心（已完成基础开发，待补审核流和上传能力）

**后端**（整合已有用户模块）：

- [x] 整合 `com.travelmate.backend.UserController` 到主应用
- [x] `Post` 实体 + Controller/Service（发布、列表、详情）
  - [ ] 图片上传（Base64 或 MultipartFile 存本地）
  - [ ] 地理位置打卡字段联调
  - [ ] 审核中状态流（当前发帖直接发布）
- [x] `Comment` 实体 + Controller/Service（二级评论基础能力）
- [x] `Like` Controller/Service（点赞/取消点赞、收藏计数）
- [x] `Follow` Controller/Service（关注/取关、粉丝/关注列表）
- [x] UserProfile Controller（基础个人信息查询/更新）

**前端页面**：

- [x] Login/Register（登录注册已合并在同页）
- [x] Community（社区发现页）
- [x] PostDetail（游记详情 + 评论）
- [x] PostCreate（发布游记）
- [x] UserProfile（个人主页基础版）

---

### 模块 E：管理后台与可观测性（已完成基础开发，待补图表与页面拆分）

**后端**：

- [x] RBAC 权限控制（`SecurityConfig` + `checkAdmin()`）
- [x] `AdminController`（基础统计、航班/酒店管理、游记审核、用户管理）
- [x] `SysLog` AOP 切面（关键操作自动记录日志）
- [x] `SensitiveWordService`（敏感词词库过滤）
- [ ] `AdminResourceController` / `AdminContentController` / `AdminMonitorController` 的职责拆分
- [ ] 更完整的 QPS / 错误日志 / 订单报表能力

**前端页面**：

- [x] AdminDashboard（基础版）
- [ ] AdminResource（独立资源管理页）
- [ ] AdminContent（独立内容审核页）
- [ ] ECharts 订单趋势 / QPS 实时大屏

---

## 五、实施优先级与下一阶段时间规划

| 优先级  | 内容                                                                | 预计用时 |
| ------- | ------------------------------------------------------------------- | -------- |
| P0 最高 | 修复前后端字段/状态不一致、后台统计字段不一致、用户状态展示错误     | 0.5天    |
| P0 最高 | 修复当前 IDE / 编译问题（`@NonNull` 注解、无用 import、Maven 同步） | 0.5天    |
| P1 高   | 补齐通知中心、订单详情页、后台独立资源/内容页                       | 1.5天    |
| P1 高   | 落实 Redis 预减库存与 AI Skills 工具调用                            | 1天      |
| P1 高   | 补齐社区审核流、图片上传与个人中心细节能力                          | 1天      |
| P2 中   | 完善监控图表（ECharts）、错误日志和报表展示                         | 1天      |
| P3 低   | 多语言、价格趋势图表增强、体验优化                                  | 0.5天    |

---

## 六、核心接口规范

所有接口统一返回格式：

```json
{
  "code": 200,
  "msg": "成功",
  "data": {}
}
```

错误码规范：

- `200` 成功
- `401` 未登录/Token 过期
- `403` 权限不足
- `500` 服务器错误
- `601` 库存不足

JWT 传递方式：`Authorization: Bearer <token>`

---

## 七、Mock 数据计划

初始化 SQL 需包含：

- 50+ 条航班数据
- 50+ 条火车数据
- 20+ 家酒店（含房型）
- 10+ 个景点
- 5+ 条游记（含图片 URL）
- 1 个管理员账号（username: admin, password: admin123）
- 1 个测试用户账号（username: test, password: test123）

---

## 八、截至 2026-05-12 的问题与未完成任务

### 8.1 文档与实现不一致

- 当前后端实际使用 Java 21，文档原先写为 Java 17。
- 当前前端实际为 Vue 3 + JavaScript，文档原先写为 TypeScript。
- 酒店、AI、社区、管理后台均已完成基础代码，但旧版计划仍写成“未开始”或“部分完成”。

### 8.2 当前已识别的问题

- `JwtFilter`、`WebConfig`、`RateLimiterInterceptor` 存在 `@NonNull` 相关 IDE / 编译告警。
- `AiServiceImpl` 存在无用 `Collectors` import。
- Maven 工程状态未与 `pom.xml` 完全同步。
- 管理后台统计接口返回字段为 `todayNewUsers`，前端页面读取的是 `todayNew`，导致“今日新增”卡片显示异常。
- 管理后台用户状态显示语义与数据库定义相反：数据库中 `0=禁用, 1=正常`，前端当前按 `0=正常` 展示。
- 酒店订单前后端字段不一致：后端使用 `guestName` / `guestPhone`，前端展示使用 `contactName` / `contactPhone`。
- 酒店订单状态语义不一致：后端与 SQL 中 `4=已取消`，前端当前将 `4` 显示为“已退款”。
- 社区发帖当前直接写入 `status=1`，未进入“待审核”状态，后台审核流不完整。

### 8.3 仍未完成的任务

- 大交通订单详情页与更完整的预订交互。
- 酒店独立下单页、Redis 预减库存、防超卖方案与文档要求对齐。
- AI 通知中心前端页面与通知发送链路。
- AI Skills / 工具调用接入真实站内数据，而不仅是普通对话。
- 社区图片上传、位置打卡、草稿与审核流增强。
- 管理后台独立资源页、内容审核页、监控图表页。
