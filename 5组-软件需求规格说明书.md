# 软件需求规格说明书

## 1 范围
### 1.1 标识
- **系统名称**：伴游（TravelMate）出行旅游平台
- **系统简称**：TravelMate
- **版本号**：V1.0
- **开发小组**：Miracle 开发小组
- **文档类型**：软件需求规格说明书（SRS）

### 1.2 系统概述
TravelMate 是面向旅游全流程的一体化平台，覆盖 **行前规划、行中履约、行后分享** 三个阶段，形成“内容-交易-服务”闭环。

系统主要能力包括：
- **资源预订**：机票、火车票、酒店与本地玩乐预订。
- **智能服务**：AI 行程规划、AI 客服 Agent、多轮问答与工具调用。
- **社区互动**：UGC 图文发布、点赞评论、关注关系与内容发现。
- **管理后台**：资源维护、内容审核、权限管理、系统可观测性。


### 1.3 文档概述
本文档用于定义 TravelMate 的功能需求、性能需求、接口需求、质量属性和验收标准，是后续设计、开发、测试与验收的共同依据。

### 1.4 基线文档
- 《软件开发计划书》V1.0（Miracle 小组）
- 《软件详细设计说明书：TravelMate（伴游）智能旅游平台》
- 《软件工程基础2026春大作业要求》

## 2 引用文件
- 《软件工程基础2026春大作业要求》
- 《软件开发计划书》V1.0（Miracle 小组）
- DeepSeek API 官方文档
- Spring Boot / Vue3 / MySQL / Redis 官方文档

## 3 需求
### 3.1 系统状态与运行方式
系统应支持以下运行状态：
- **正常运行状态**：完整支持检索、AI 规划、下单、支付（模拟）、社区发布。
- **维护状态**：允许资源批量更新、审核策略调整与数据维护。
- **降级状态**：外部 AI 接口超时或失败时，返回本地推荐模板并保留主流程可用。

### 3.2 需求概述
#### 3.2.1 总体目标
- **目标一**：构建一站式旅游交易履约主链路（查询-下单-支付-履约-评价）。
- **目标二**：建设 AI Native 的出游规划与客服能力。
- **目标三**：建立 UGC 社区，形成“内容驱动交易转化”的业务闭环。
- **目标四**：提供可观测、可治理的管理后台。

#### 3.2.2 核心业务流程
**用户输入需求 -> AI 生成结构化行程 -> 匹配站内资源 -> 下单预占库存 -> 模拟支付 -> 订单履约 -> 社区分享与评价**。

#### 3.2.3 运行环境
- **前端**：Vue 3 + Vite，支持 Chrome / Edge 主流浏览器。
- **后端**：Java 17 + Spring Boot 3.x + MyBatis-Plus。
- **数据层**：MySQL 8.0 + Redis。
- **部署层**：Nginx 反向代理与静态资源托管。

#### 3.2.4 用户特征
- **普通游客**：关注效率与易用性，希望快速得到可执行行程。
- **内容创作者**：需要便捷发布图文/位置打卡并获得互动反馈。
- **平台管理员**：关注内容安全、资源维护与运行监控。

#### 3.2.5 关键约束
- **周期约束**：按课程周次推进，满足阶段性交付。
- **技术栈约束**：使用 Vue3、Spring Boot 3.x、MySQL、Redis。
- **边界约束**：不接真实票务和真实支付，采用 Mock 与本地数据。
- **AI 约束**：以 DeepSeek OpenAI 兼容接口为主，输出需可结构化校验。
- **验收约束**：关键需求需对应可量化指标与验证方法。

### 3.3 系统功能与子系统划分
#### 3.3.1 总体结构
系统采用前后端分离架构，分为：
- 表现层（Web/H5）
- 业务服务层（REST API）
- 数据与缓存层（MySQL/Redis）

#### 3.3.2 子系统说明
- **A 大交通票务与订单子系统**：航班/车次查询、价格趋势、订单流转。
- **B 住宿与本地生活子系统**：酒店搜索、房态管理、景点核销、评价体系。
- **C AI 智能规划与 Agent 子系统**：行程生成、智能问答、通知服务。
- **D 社区与用户中心子系统**：JWT 鉴权、UGC 发布、社交关系链。
- **E 管理后台与可观测性子系统**：RBAC、监控看板、审核流与资源管理。

#### 3.3.3 数据与命名约定
- 时间字段统一为 **create_time / update_time**。
- 数据库命名采用 **snake_case**，代码变量采用 **camelCase**。
- 金额统一使用 **分（int）** 存储，避免浮点误差。

### 3.4 CSCI 能力需求
#### 3.4.1 AI 行程生成
- 输入：目的地、天数、预算、人数、偏好。
- 输出：按天行程、推荐资源列表、预算摘要。
- 业务规则：
	- 系统应优先返回结构化行程结果，并支持“重新生成”与“手动微调”。
	- 当 AI 返回结构不合法（字段缺失或格式错误）时，系统应进入降级流程。
	- 降级时默认展示“热门推荐模板”，并提示“智能规划暂不可用，可继续浏览推荐方案”。
	- 降级状态下用户仍可继续收藏、加入清单与下单，主流程不得中断。

#### 3.4.2 并发下单与库存控制
- 业务目标：系统需保证高并发下单场景中库存不超卖，订单与库存保持最终一致。
- 业务规则：
	- 支持至少 **50 QPS** 的并发下单请求。
	- 并发提交时不得出现负库存。
	- 超时未支付订单（15 分钟）必须自动释放库存。
	- 库存变化与订单状态变化需可追踪、可审计。
- **说明**：具体实现机制属于设计内容，不在本 SRS 展开。

#### 3.4.3 订单状态机控制
- 核心流转：**待支付 -> 已支付/已取消 -> 已出行 -> 已评价/退款处理**。
- 每次状态变更必须校验预期状态，防止乱序写入。

#### 3.4.4 资源匹配与缺失处理
- 当 AI 生成地点无法匹配站内资源时，系统应保留地点文本并标记“暂无可预订资源”。
- 系统应提供同区域候选资源（如存在），并允许用户手动替换。
- 部分地点匹配失败不得导致整条行程生成失败。

#### 3.4.5 模拟支付规则
- 用户点击“确认模拟支付”并完成二次确认后，系统判定支付成功。
- 支付成功后订单状态应切换为“已支付”，并可继续履约流程。
- 取消确认或超时未支付时，订单维持“待支付”并按规则关闭。

#### 3.4.6 社区内容安全
- 敏感词自动过滤 + 人工复核。
- 举报闭环可追踪，违规内容可快速下架。

### 3.5 外部接口需求
#### 3.5.1 接口对象
- DeepSeek API（AI 能力）
- 地图 API（位置展示）
- 文件服务（图片上传与访问）

#### 3.5.2 用户接口
- 前台应采用卡片式信息架构，强调路径清晰与反馈及时。
- 核心页面最低元素要求如下：

| 页面 | 必须包含的元素 |
|---|---|
| 行程结果页 | 地图路线预览区、按天行程卡片、总预算模块、一键加入清单按钮 |
| 资源详情页 | 资源图片区、价格与余量区、规则说明区、立即下单按钮 |
| 订单页 | 状态筛选区、订单列表、支付入口、取消入口、状态时间线 |
| 社区发布页 | 图文输入区、地理位置签到、话题标签、发布按钮 |
| 管理审核页 | 待审核列表、内容预览区、通过/驳回操作区、操作日志区 |

- 需提供上述页面低保真原型（Low-fi）作为验收附件。

#### 3.5.3 软件接口
- 协议：HTTP/HTTPS，RESTful。
- 数据格式：JSON。
- 鉴权：Bearer Token（JWT）。
- 支付：**仅支持内部模拟支付回调，不接真实网关**。

### 3.6 内部接口需求
- AI 子系统需调用票务/酒店资源检索接口。
- 社区子系统发布内容后需进入管理后台审核流。
- 统一错误码与响应结构，便于前后端联调。

### 3.7 内部数据需求
核心实体至少包括：用户、行程、资源、订单、消息、社区内容、审核记录、系统日志。

#### 3.7.1 核心数据字典（关键字段）

| 实体 | 关键字段 | 字段说明 |
|---|---|---|
| User | user_id, phone, password_hash, role, create_time | 用户唯一标识、手机号、加密密码、角色、创建时间 |
| Itinerary | itinerary_id, user_id, destination, days, content_json, status | 行程ID、所属用户、目的地、天数、结构化内容、状态 |
| Resource | resource_id, resource_type, name, city, price_cent, stock, status | 资源ID、类型、名称、城市、价格（分）、库存、状态 |
| Order | order_id, user_id, order_no, total_amount_cent, order_status, expire_time | 订单ID、用户ID、订单号、金额（分）、状态、过期时间 |
| Post | post_id, user_id, title, content, location_text, review_status | 内容ID、用户ID、标题、正文、位置文本、审核状态 |
| ReviewTask | task_id, target_type, target_id, result, reviewer_id, review_time | 审核任务ID、目标类型、目标ID、结果、审核人、审核时间 |

### 3.8 适应性需求
- 前端使用路由懒加载与资源分包。
- 弱网场景下应优先保证主流程页面可用。

### 3.9 安全与隐私需求
- **认证**：基于 JWT 的会话认证。
- **授权**：基于 RBAC 的权限控制。
- **存储**：用户密码必须哈希存储（如 BCrypt）。
- **脱敏**：隐私字段在接口层按角色脱敏返回。
- **审计**：关键管理操作需记录操作日志。

### 3.10 环境与资源需求
#### 3.10.1 硬件需求
- CPU：2 核及以上。
- 内存：4GB 及以上。
- 磁盘：建议 SSD，预留 20GB 以上。
- 网络：可稳定访问外部 AI 与地图接口。

#### 3.10.2 软件需求
- 服务端：Linux（Ubuntu/CentOS）优先。
- 运行环境：JDK 17、Node.js 18+、Nginx 1.20+。
- 中间件：MySQL 8.0、Redis 6.2+。

#### 3.10.3 通信与性能需求
- 非 AI 关键接口（下单、查询订单、发布内容）在稳态下 **P95 响应时间 < 200ms**。
- AI 生成响应时间：**3-10s**（受模型与网络影响）。
- 并发能力：支持至少 **50 QPS** 的并发下单请求。
- 一致性要求：并发压测下库存不得超卖且不得出现负库存。

### 3.11 质量属性需求
- **可靠性**：数据库异常恢复后，未支付订单库存释放准确率应达到 **100%**。
- **可维护性**：需求项需具备唯一编号并可映射至设计文档、测试用例和缺陷单。
- **易用性**：行程生成主流程控制在 3 步以内。
- **可观测性**：后台可查看 QPS、错误率、关键业务指标。

### 3.12 降级策略
| 场景 ID | 触发条件 | 降级策略 | 用户可见反馈 | 恢复条件 |
|---|---|---|---|---|
| DEG-AI-001 | AI 接口超时或返回结构非法 | 启用本地静态模板并保留编辑能力 | 智能规划暂不可用，已切换推荐方案 | AI 服务健康检查通过 |
| DEG-MAP-001 | 地图服务加载失败 | 隐藏地图组件，仅显示文字地址与顺序列表 | 地图暂不可用，已切换文字视图 | 地图服务恢复 |
| DEG-CACHE-001 | 缓存层不可用 | 切换数据库直查并启用限流保护 | 系统繁忙，请稍后重试 | 缓存层恢复且稳定 |

### 3.13 设计与实现约束
- 保持前后端分离与 RESTful 设计。
- 关键业务接口需具备幂等或防重处理。
- 不引入与课程目标无关的重型中间件，优先轻量可复现方案。

### 3.14 数据需求
- 输入数据：注册登录信息、行程偏好、订单操作、UGC 内容。
- 输出数据：行程 JSON、订单状态、通知消息、社区内容流。
- 数据规模：支持万级订单与十万级 Mock 资源检索。

### 3.15 操作需求
- 初始化：通过 init.sql 导入结构与基础数据。
- 日常：登录、检索、规划、下单、支付（模拟）、发帖。
- 恢复：服务重启后可快速恢复核心能力；超时未支付订单需自动回滚库存。

### 3.16 故障处理需求
- **AI 故障**：返回本地模板并提示稍后重试。
- **库存冲突**：返回库存不足并终止下单。
- **认证失效**：返回登录过期并引导重新认证。
- **幂等冲突**：重复请求不得造成重复扣库存或重复创建订单。

建议标准错误码：
- `ERR_001`：AI 服务暂不可用
- `ERR_002`：库存不足，订单关闭
- `ERR_003`：登录过期，请重新认证

### 3.17 规则说明（非实现细节）
- **AI 结果规则**：输出需满足系统定义数据结构，不合法时进入降级流程。
- **库存一致性规则**：库存与订单状态保持一致，不得出现超卖与负库存。

### 3.18 人员、培训与后勤需求
- 用户需具备基本浏览器操作能力。
- 开发维护人员需掌握 Git、Spring Boot、数据库基础。
- 需交付用户操作指南与部署维护说明。

### 3.19 交付与包装需求
交付物应包含：
- 源码（含前后端）
- 数据库初始化脚本
- 运行与部署说明（README）
- 演示视频
- 阶段文档（计划、需求、设计、测试）

## 4 合格性规定
| Requirement ID | 需求项 | 需求说明 | 合格性方法 |
|---|---|---|---|
| REQ-FUNC-001 | **功能完整性** | 实现从需求输入到行程生成再到下单的业务闭环 | 演示全流程并记录结果 |
| REQ-PERF-001 | **并发稳定性** | 50 线程并发下单库存不超卖且无负库存 | JMeter 压测 + 对账日志核验 |
| REQ-API-001 | **接口规范性** | RESTful + JSON + 统一响应结构与错误码 | 审查 Swagger/Apifox 与联调记录 |
| REQ-SEC-001 | **安全性** | 密码哈希、JWT 鉴权、RBAC 控制、敏感字段脱敏 | 代码审查 + 抓包与数据库抽检 |
| REQ-REL-001 | **降级能力** | AI 或地图能力异常时主流程可继续且页面不空白 | 故障注入测试 + 页面录屏 |
| REQ-CONS-001 | **状态一致性** | 状态机流转合法，超时订单可回滚库存 | 集成测试 + 状态迁移日志核验 |
| REQ-UI-001 | **界面完整性** | 核心页面满足最小元素集合并提供低保真原型 | 原型审查 + 页面走查记录 |

## 5 需求可追踪性
- 本文档需求可追踪到《软件开发计划书》的目标与阶段成果。
- 模块级需求可追踪到《软件详细设计说明书》的模块设计与接口定义。
- 测试项应与本说明书第 4 章合格性规定一一对应。

## 6 尚未解决的问题
- 第三方 AI 接口偶发超时，需持续优化重试与降级策略。
- 当前支付为模拟模式，如实际上线需补充真实支付合规方案。

## 7 注解
- **CSCI**：Computer Software Configuration Item，软件配置项。
- **UGC**：User Generated Content，用户生成内容。
- **JWT**：JSON Web Token，认证令牌。
- **RBAC**：Role-Based Access Control，基于角色的访问控制。
- **地理位置签到**：用户发布内容时附加地理位置信息的操作。
- **内容转化**：用户从浏览内容转为收藏、下单或预约的行为。

## 附录
无
