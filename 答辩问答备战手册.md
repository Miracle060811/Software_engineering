# 软件工程基础实践 · 答辩问答备战手册

> 项目：TravelMate（旅行助手系统）｜ 答辩时间：9月4日 14:00-17:00 ｜ 每组15分钟（项目讲解4分钟 + 演示7分钟 + 问答4分钟）
> 本文档按"老师可能问什么"组织，每个问题给出答案要点和关键数字。**所有数字都来自项目实际记录，背下来。**

***

## 一、项目与架构总览（开场必问）

**Q：你们的项目是做什么的？整体架构是什么？**

- TravelMate 旅行助手系统，前端 Vue 3 + Vite，后端 Java 21 + Spring Boot 3.5.15 + MyBatis-Plus，数据库 MySQL 8.0，Redis 用于缓存/限流/库存辅助。

- 保留了改造前的单体版本（Git 标签 `monolith-start`，commit `7258cd2...`），作为性能对比基线。

- 改造后拆成 **6 个业务微服务**：identity（身份/用户）、traffic（交通票务）、local（本地/酒店）、ai（AI 行程）、community（社区/游记）、ops（运营）。

- 每个服务独立构建、独立部署、独立数据库（Database per Service），共 6 套 MySQL + 1 个共享 Redis。

**Q：为什么拆成 6 个服务？拆分依据是什么？**

- 按**业务领域边界**拆分（DDD 限界上下文思路），不是按用例数量机械拆分：

  - identity：用户注册登录、旅客管理、JWT 认证 —— 被其他服务依赖的基础服务；

  - traffic：航班/火车票查询与下单、候补 —— 高并发读多写少；

  - local：酒店查询与下单、本地生活服务 —— 有防超卖库存竞争；

  - ai：AI 行程生成 —— 依赖外部大模型，需要隔离降级；

  - community：游记发布与审核 —— 内容类业务；

  - ops：运营管理。

- **数据归属原则**：每张业务表明确归一个服务管理，其他服务不能直接读写；6 个库（travelmate\_identity / traffic / local / ai / community / ops），数据库初始化脚本里给每个服务建了独立账号，只授权访问自己的库（见 `03_devops/kubernetes/microservice-database-bootstrap.yaml`）。

**Q：服务之间怎么调用？调用失败怎么办？**

- 同步 REST 调用（如 traffic-service 创建火车订单前调用 identity-service 校验旅客归属）。

- 调用失败有设计好的处理：连接超时 1000ms、读取超时 2000ms；超时或 5xx 时转换为 HTTP 503，返回固定提示"身份服务暂不可用，请稍后重试"，**不产生脏订单、不错误扣库存**（这是故障处理实验验证过的）。

- 另外还有 Outbox 模式开关处理跨服务数据一致性（ConfigMap 中有 `OUTBOX` 配置）。

***

## 二、Kubernetes 部署（高频必问）

**Q：K8s 是怎么部署的？说一下完整流程。**

1. 集群：本机 Docker Desktop 自带 Kubernetes（v1.36.1），5 个节点全部 Ready。
2. 所有资源在 `travelmate` 命名空间下（`03_devops/kubernetes/namespace.yaml`）。
3. 资源清单：`03_devops/kubernetes/` 下有 namespace、ConfigMap（`microservices-configmap.yaml`，含各服务 URL、Redis 地址、DB 连接）、数据库 bootstrap Job、6 个微服务的 Deployment + Service、HPA、前端 Deployment + Service。
4. 镜像：推送到 GHCR（GitHub Container Registry），带版本号（commit SHA），**不是 latest**。
5. 部署方式：流水线自动部署；本地也可运行 `03_devops/scripts/Deploy-TravelMateMicroservices.ps1`，脚本复制 ConfigMap/HPA/各服务 manifest，生成 kustomization 后 `kubectl apply`。
6. 访问：前端 Service 是 LoadBalancer 类型，外部端口 **30080**。

**Q：现场怎么证明部署成功？（演示命令）**

```powershell
kubectl config current-context          # 应为 docker-desktop
kubectl get ns                           # 有 travelmate
kubectl get deploy -n travelmate         # 6 个微服务 + 前端
kubectl get pods -n travelmate           # 全部 Running/Ready
kubectl get svc -n travelmate            # 前端 LoadBalancer 30080
kubectl get hpa -n travelmate            # 6 个 HPA
curl http://localhost:8081/actuator/health   # 健康检查（各服务 8081-8086）
```

**Q：健康检查、就绪检查怎么做的？**

- Spring Boot Actuator 暴露 `/actuator/health`；K8s Deployment 里配置了 livenessProbe 和 readinessProbe，探针不通过 Pod 不会接流量。

- 镜像带版本号（commit SHA），`kubectl describe pod` 可现场看到镜像版本。

**Q：如果一次部署失败了，你们怎么排查的？**

- 答思路：`kubectl get pods` 看状态 → `kubectl describe pod` 看事件（镜像拉取失败/探针失败）→ `kubectl logs` 看应用日志 → 检查 ConfigMap/Secret 配置 → 必要时回滚（重新 apply 旧版本镜像 tag）。CI 里任何一步失败后续部署不会继续，失败记录保留在 GitHub Actions。

***

## 三、HPA 自动扩缩容实验（必问）

**Q：HPA 怎么配置的？**

- 6 个微服务全部配置 HPA（`03_devops/kubernetes/hpa.yaml`）：**minReplicas=2，maxReplicas=6，CPU 平均利用率目标 60%**，并配置了扩缩容行为参数（scale-down 稳定窗口，防止抖动）。

- 每个 Deployment 设置了 resources requests/limits（HPA 基于 CPU 利用率的前提是 requests 已设置）。

**Q：扩缩容实验怎么做的？结果如何？**（记录见 `04_tests/stress/results/HPA实验记录-20260902.md`）

- 用 **k6** 对 traffic-service 施压（50 VU）。

- 压力上升：CPU 峰值冲到 **237%** → HPA 期望副本数升至 **6** → Ready Pod 从 **2 扩到 4**。

- 压力停止：副本最终**缩回 2**。

- 期间指标：4252 个请求、31.43 req/s、平均 5.09ms、P95 7.27ms、错误率 0%。

- 关键表述：扩容有滞后（HPA 采样周期 + Pod 启动时间），这是正常现象，我们配置了稳定窗口避免频繁抖动。

***

## 四、压力测试与性能对比（必问，数字要背）

**Q：压力测试怎么做的？**

- 工具：**k6**（脚本在 `04_tests/stress/`），负载模型为 **5→10→20→50 VU 阶梯加压**。

- 三个场景：航班查询、酒店查询、并发下单（防超卖）。

- 口径（评分标准要求"条件相同"）：**同一台机器（i5-1340P / 15.7GiB）、同一批数据、同一份脚本、每场景每版本跑 3 次取中位数**；单体跑在 `monolith-start` 标签版本（宿主 Java 进程 18080 端口 + 隔离 MySQL 13306），微服务用独立 Compose project。

**Q：性能对比结果是什么？**（记录见 `04_tests/stress/results/performance-2026-09-02/README.md`）

| 场景             | 指标（中位数）  | 单体                  | 微服务                |
| -------------- | -------- | ------------------- | ------------------ |
| 航班查询           | 平均 / P95 | 13.25ms / 20.53ms   | 3.05ms / 4.60ms    |
| 酒店查询           | 平均 / P95 | 14.91ms / 22.79ms   | 2.92ms / 4.44ms    |
| 并发下单(50VU抢库存1) | 平均 / P95 | 271.18ms / 801.52ms | 74.11ms / 139.68ms |

- 错误率全部 0%，k6 退出码全部 0。

- 微服务航班查询 P95 比单体低约 **77.6%**，酒店查询 P95 低约 **80.5%**。

**Q：可能的追问——为什么微服务反而更快？吞吐量呢？**

- 诚实回答（报告原文口径）：脚本里每次迭代有固定 `sleep(0.5)` 思考时间，**吞吐量被思考时间封顶**（单体/微服务都约 31 req/s，差异仅 1.9%–2.5%），不能据此宣称最大吞吐能力；响应时间差异来自微服务只加载自己领域的依赖、连接池和缓存更聚焦，且单体查询走了更重的整体上下文。

- CPU/内存两版采集口径不同（进程采样 vs docker stats），只做运行状态描述，不做百分比优劣结论 —— 这种严谨表述是加分项。

**Q：防超卖怎么验证的？**

- 50 个并发用户同时抢库存为 1 的房型，单体和微服务各跑 3 次：成功订单数恒等于 1，库存扣到 0 不为负，DB 有效订单数一致，check 通过率 100/100。

- 机制：数据库乐观/悲观控制 + Redis 预扣库存（`hotel:room:stock:{id}` 键）。脚本带自动断言（成功订单数 ≤ 初始库存），机器判定。

***

## 五、故障处理实验（必问）

**Q：故障处理做了什么？**（报告见 `04_tests/fault/TravelMate-Kubernetes故障处理实验报告.md`）

- 场景：traffic-service 创建火车订单前要调 identity-service 校验旅客归属。

- 注入故障：K8s 中**临时删除 identity-service 的 HPA，把 Deployment 缩容到 0**（脚本 `Invoke-IdentityOutageExperiment-k8s.ps1`，2026-09-02 执行，结果：通过）。

- 验证点：

  1. 订单接口及时返回设计好的降级提示（HTTP 503 + "身份服务暂不可用，请稍后重试"），**不是长时间挂起**（连接超时 1s / 读取超时 2s）；
  2. 故障请求不产生异常订单、不错误扣库存（零脏数据）；
  3. 其他 5 个微服务不跟着崩溃（故障隔离）；
  4. identity 恢复后系统整体恢复；实验结束恢复 HPA 和副本数。

- 原始证据：JSON 结构化结果 + 完整日志在 `04_tests/fault/results/`。

***

## 六、CI/CD 流水线（必问）

**Q：流水线怎么做的？push 之后发生什么？**

- GitHub Actions，`.github/workflows/ci.yml`（另有 codeql.yml 安全扫描、security.yml）。

- 触发：push 到指定分支 / PR / 手动 dispatch。

- Job 链（按依赖顺序）：

  1. **changes**：检测改动范围（backend/frontend/microservices），只跑相关部分；
  2. **validation**：仓库校验（UC01-UC19 E2E 结构校验、数据库迁移和 K8s manifest 校验、kubectl kustomize 渲染验证、部署脚本语法检查）；
  3. **backend**：单体后端测试 + 打包（真实 MySQL/Redis service 容器跑集成测试，空库验证 Flyway 自动建表）；
  4. **microservices**：六服务 Maven reactor 一次构建，101 个测试 + 契约测试，PR 上构建镜像但不发布；
  5. **frontend** → **real-e2e**：真实浏览器 E2E；
  6. **ci-gate** → **release-security-gate** → **microservice-publish**（镜像推 GHCR，带 commit SHA 版本）→ **images** → **deploy**（部署到 K8s + 健康检查）→ **delivery-gate**。

- **任何一步失败，后续部署不继续**（needs 依赖链保证），成功/失败记录都保留在 Actions 历史。

- E2E 微服务版 2026-09-02 在流水线跑通 **17/17**（run 33582799133）。

**Q：镜像版本策略？** —— 用 commit SHA 打 tag 推 GHCR，不用 latest，可回溯可回滚。

***

## 七、测试体系（必问）

**Q：测试怎么补齐的？规模多少？**（索引见 `04_tests/README.md`）

| 测试类型     | 位置                          | 规模              | 说明                                                 |
| -------- | --------------------------- | --------------- | -------------------------------------------------- |
| 单体单元+集成  | `backend/src/test/`         | 47 个测试类 205 个方法 | Failsafe 在真实 MySQL 容器跑集成测试；覆盖重复支付拒绝、库存竞态回滚、越权隔离    |
| 微服务单元+契约 | `microservices/*/src/test/` | 约 101 个测试       | 115 个端点（94 公开 + 19 内部 + 跨服务 503 锚点）都有 MockMvc 契约测试 |
| 前端单元     | `frontend/`                 | 26 个用例          | Vitest                                             |
| E2E      | `frontend/tests/e2e-real/`  | 17 个场景          | Playwright 真实浏览器，覆盖 UC01–UC19 全部用例                 |

- 单元测试**有断言**（判断结果，不只看报不报错）；测试报告写清总数/通过数/失败原因/运行环境（见 `02_docs/测试执行报告-*.md` 多日期版本）。

- 用例追溯：19 个用例全部 covered、0 待测（历史门禁校验报告在 `04_tests/reports/ci/`）。

- 数据迁移验证：单体→六服务迁移 31/31 张表行数一致（`04_tests/migration/`）。

***

## 八、需求、文档与追溯

**Q：用例清单和追溯怎么做的？**

- 第 1 天列出完整业务场景清单：**UC01–UC19 共 19 个用例**，经助教确认即为验收范围。

- 每个用例有用例说明（参与者、触发、前置、主成功流、备选/异常流、可验证结果），并画了系统级/组件级/对象级三层模型图。

- 追溯链编号：`REQ → UC → SYS-SEQ → COMP-SEQ → OBJ-SEQ → UNIT-TC → INT-TC → E2E-TC`，汇总在《需求设计代码测试追溯表》。

- **答辩重点讲解的 3 个代表性用例**：**UC03 查询预订火车票或候补**、**UC11 AI行程生成与保存**（外部 AI 异常时本地模板 fallback 降级）、**UC14 游记发布与审核**（敏感词校验 + 管理员审核流）。

**Q：文档清单** —— 需求规格说明书、概要设计（组件图）、详细设计（类图）、测试计划、测试报告、追溯表、部署文档、开发计划书、业务场景清单、大模型使用说明，均在 `02_docs/`（md + pdf + mermaid 源文件）。

***

## 九、项目管理（可能问）

- 每天 9:00 站会（昨天做了什么/今天做什么/卡在哪），12:00 前提交约 200 字简报（`05_management/`）。

- 看板 + 每日截图（带日期）；任务有编号、对应需求/用例、负责人、计划/实际时间、验收条件、证据链接。

- 重要代码走 PR + 组员评审；Git 提交数不作为个人贡献唯一标准。

- 个人权重 0.9–1.1，全组之和 ≤ 人数，全员签字确认。

***

## 十、刁钻追问预案

| 追问                     | 应对                                                                                           |
| ---------------------- | -------------------------------------------------------------------------------------------- |
| 微服务一定比单体好吗？            | 不一定。课程也不要求更快。我们的结果是"本轮固定脚本下响应时间更低"，吞吐受思考时间封顶；微服务的收益在独立部署、独立扩缩容、故障隔离，代价是运维复杂度和跨服务调用开销。        |
| 6 个服务是不是过度拆分？          | 按业务领域拆分，每个服务有明确边界和独立数据；拆分前评估过用例聚类，traffic/local 并发特征不同所以分开。                                  |
| 数据库为什么不用一个库不同 Schema？  | 课程允许同服务器不同库；我们用独立 MySQL 实例（Compose 里 3307-3312 六个实例）实现更强的隔离，K8s 里用 bootstrap Job 建库建账号、最小授权。 |
| 跨服务一致性怎么保证？            | 同步调用 + 超时降级（traffic→identity 案例），Outbox 模式处理异步一致性。                                           |
| AI 用在哪了？怎么保证诚信？        | 技术总结报告里写了用了什么工具、做什么、怎么人工检查（`02_docs/大模型使用说明.md`）；所有 AI 产出都人工运行验证过；仓库无密钥（用 Secret/环境变量）。      |
| 部署失败怎么查？               | get pods → describe pod 看事件 → logs 看日志 → 查探针/ConfigMap → 旧版本镜像回滚。                            |
| 为什么 HPA 阈值 60%？副本 2-6？ | 60% 留突发余量又不太浪费；min 2 保证高可用（单 Pod 故障仍有副本），max 6 受本机资源约束。                                      |

***

## 十一、数字速记卡（考前过一遍）

- **6** 个微服务：identity/traffic/local/ai/community/ops，端口 **8081–8086**，MySQL **3307–3312**

- **19** 个用例（UC01–UC19），E2E **17 场景**全过

- 测试：单体 **205** 个测试方法（47 类）+ 微服务约 **101** 个 + 前端 **26** 个；契约覆盖 **115** 端点

- HPA：**2–6 副本，CPU 60%**；实验：CPU 峰值 **237%**，Pod **2→4→2**，P95 **7.27ms**，错误率 0%

- 性能：航班 P95 单体 20.53ms → 微服务 4.60ms（↓77.6%）；酒店 ↓80.5%；下单 271→74ms

- 故障：identity 缩容到 0 → 503 降级"身份服务暂不可用"，超时 1s/2s，零脏数据

- 迁移：31/31 表行数一致

- 前端 K8s 访问端口 **30080**；单体标签 `monolith-start`

