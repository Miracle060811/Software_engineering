# TravelMate 测试报告

## 1 引言

### 1.1目标

本报告用于记录 TravelMate（伴游）出行旅游平台在本轮全量重测中的测试范围、测试项、执行日志、实际结果、未覆盖风险和最终结论。报告中的“通过”均来自本次实际执行结果；未自动验证的内容标注为“未覆盖”或“需人工复核”。

### 1.2背景

TravelMate 是面向出行旅游场景的一体化课程项目，覆盖用户认证、大交通票务、酒店景点、AI 行程规划、社区互动、管理后台和启动部署脚本。由于系统包含前后端分离、数据库、Redis、JWT 鉴权和外部 AI 服务等多个组成部分，需要通过自动化测试、构建验证和启动脚本验证确认核心功能可重复运行。

### 1.3范围

本报告覆盖当前仓库根目录下的后端、前端和启动脚本：

| 范围 | 覆盖内容 | 本轮状态 |
| --- | --- | --- |
| 后端自动化 | Spring Boot 上下文、MockMvc、安全拦截、公开接口、统一响应、库存状态边界 | 已测试，通过 |
| 前端自动化 | Playwright 页面 smoke、登录流程、路由守卫、非管理员拦截 | 已测试，通过 |
| 前端构建 | Vite 生产构建 | 已测试，通过，有构建警告 |
| 启动脚本 | `start.ps1 -DryRun` 参数解析和启动命令输出 | 已测试，通过；16:09 复测时 Redis 已监听 |
| 完整业务端到端 | 真实下单、支付、退款、审核、后台 CRUD | 核心下单与审核已补测；后台完整 CRUD 仍需人工回归 |
| 高并发压测 | 真实并发库存扣减和回补 | 已补测，通过 |
| 外部 AI 稳定性 | DeepSeek API 连通、行程生成、客服对话和无有效 Key 降级 | 已补测，通过；长时间稳定性仍需最终验收复核 |

### 1.4 引用文件

| 文件 | 说明 |
| --- | --- |
| `5组-软件需求规格说明.md` | 需求范围和合格性规定 |
| `5组-软件详细设计说明.md` | 模块设计和测试计划参考 |
| `README.md` | 当前实现状态、运行方式和环境要求 |
| `AGENTS.md` | 项目技术栈、启动方式、认证、库存、AI 降级等约束 |
| `docs/test-runbook.md` | 本轮新增自动化测试运行说明 |
| `backend/src/test/java/com/travelmate` | 后端测试代码 |
| `frontend/tests/e2e/travelmate-smoke.spec.js` | 前端 Playwright 测试代码 |
| `scripts/run-tests.ps1` | 本轮统一测试入口 |

## 2 测试计划

### 2.1目的

本轮测试目的为补齐可重复运行的测试代码，并对当前系统进行一次自动化重测，形成可追溯的课程测试报告。报告不展开具体测试方法，详细运行步骤见 `docs/test-runbook.md` 和测试代码。

### 2.2详细说明

#### 2.2.1 测试计划标识符

`TM-TEST-20260606-FULL-REGRESSION`

#### 2.2.2引言

本次测试面向 TravelMate 当前仓库版本，重点验证后端核心接口、权限边界、统一响应结构、前端核心页面、路由守卫、生产构建和启动脚本 DryRun。测试执行时间为 2026-06-06，测试地点为本地 Windows 开发环境。

#### 2.2.3 测试项

| 编号 | 测试项 | 对应文件/命令 | 状态 |
| --- | --- | --- | --- |
| TP-01 | 后端 JUnit/MockMvc 自动化 | `backend\mvnw.cmd test` | 已通过 |
| TP-02 | 前端依赖安装 | `cd frontend; npm install` | 已通过 |
| TP-03 | 前端生产构建 | `cd frontend; npm run build` | 已通过，有 warning |
| TP-04 | 前端 Playwright E2E | `npx playwright test --reporter=list --workers=1` | 已通过 |
| TP-05 | 启动脚本 DryRun | `.\start.ps1 -DryRun` | 已通过，有 Redis 提醒 |
| TP-06 | 全量测试脚本 | `.\scripts\run-tests.ps1` | 已通过 |

#### 2.2.4 要测试的特征

| 特征 | 覆盖结果 |
| --- | --- |
| 主启动类和 Spring Boot 上下文 | 已覆盖 |
| 公开接口统一响应 | 已覆盖 |
| 匿名访问安全拦截 | 已覆盖 |
| 管理员路由守卫 | 已覆盖 |
| 登录页面和 token 写入 | 已覆盖 |
| 前端公开页面渲染 | 已覆盖 |
| 生产构建可用性 | 已覆盖 |
| 启动脚本参数解析 | 已覆盖 |

#### 2.2.5 不要测试的特征

| 特征 | 本轮处理 |
| --- | --- |
| 真实支付网关 | 项目当前为模拟支付，不测试真实支付 |
| 真实票务/酒店外部接口 | 项目当前使用本地业务数据，不测试第三方真实接口 |
| DeepSeek 长时间稳定性 | 已补测单次正常调用和无有效 Key 降级；长时间稳定性受 API Key、网络和额度影响，仍建议最终验收复核 |
| 高并发压力指标 | 已补测云端酒店单房型 50 并发；完整阶梯性能和多资源压测仍可继续扩展 |
| 管理后台完整人工审核体验 | 需人工验收，本轮未覆盖 |

### 2.3测试方法

详细测试方法和命令已独立记录在 `docs/test-runbook.md`，本报告仅记录执行结果。

### 2.4测试通过准则

| 测试项 | 通过准则 | 本轮结果 |
| --- | --- | --- |
| 后端自动化 | JUnit 总数无失败、无错误 | 通过 |
| 前端构建 | Vite 构建退出码为 0 | 通过 |
| Playwright E2E | 所有 E2E 用例通过 | 通过 |
| 启动脚本 DryRun | 正确输出后端/前端启动命令 | 通过 |
| 未覆盖项 | 不写为通过，明确标注风险 | 已标注 |

### 2.65测试任务

| 任务 | 结果 |
| --- | --- |
| 新增后端测试代码 | 已完成 |
| 新增前端 Playwright 测试代码 | 已完成 |
| 新增统一测试运行脚本 | 已完成 |
| 新增测试运行说明 | 已完成 |
| 执行全量重测 | 已完成 |
| 重构根目录测试报告 | 已完成 |

### 2.76环境要求

| 项目 | 本次环境 |
| --- | --- |
| 操作系统 | Windows，本地 PowerShell |
| 后端运行环境 | 已在 Java 21.0.11 执行后端测试和启动脚本 DryRun；历史回归曾使用 Java 25.0.1，项目编译目标 Java 21 |
| 后端框架 | Spring Boot 3.5.13 |
| 数据库 | MySQL `travelmate`，测试中连接成功 |
| Redis | 16:09 复测时已监听 `127.0.0.1:6379`；16:52 压测前置检查时未监听 |
| 前端运行环境 | Node.js + npm |
| 前端框架 | Vue 3 + Vite 5.4.21 |
| E2E 浏览器 | Playwright Chromium 148.0.7778.96 |

## 3 测试设计说明

### 3.1 目的

本章说明本轮自动化测试覆盖的功能特征和判定结果。具体代码位于 `backend/src/test/java/com/travelmate` 与 `frontend/tests/e2e`。

### 3.2 详细说明

#### 3.2.1 测试设计说明标识符

`TM-TDS-20260606-AUTO`

#### 3.2.2要测试的特征

| 编号 | 特征 | 测试文件 | 结果 |
| --- | --- | --- | --- |
| TDS-01 | Spring Boot 主上下文加载 | `BackendApplicationTests` | 通过 |
| TDS-02 | 匿名访问写接口拦截 | `SecurityConfigTests` | 通过 |
| TDS-03 | 静态资源和 CORS 基础行为 | `SecurityConfigTests` | 通过 |
| TDS-04 | 公开 GET 接口统一响应 | `PublicApiSmokeTests` | 通过 |
| TDS-05 | 登录失败业务错误 | `PublicApiSmokeTests` | 通过 |
| TDS-06 | 酒店/景点/AI 写接口匿名拦截 | `PublicApiSmokeTests` | 通过 |
| TDS-07 | `Result<T>` 响应结构 | `ResultAndStockBoundaryTests` | 通过 |
| TDS-08 | 库存预扣减状态枚举边界 | `ResultAndStockBoundaryTests` | 通过 |
| TDS-09 | 前端公开页面渲染 | `travelmate-smoke.spec.js` | 通过 |
| TDS-10 | 前端登录和路由守卫 | `travelmate-smoke.spec.js` | 通过 |
| TDS-11 | AI 正常调用 | 本地已启动后端 + 有效 DeepSeek Key | 通过 |
| TDS-12 | AI 降级路径 | 临时 18080 后端 + `sk-demo-placeholder` 占位 Key | 通过 |

#### 3.2.3方法细化

测试执行细节不写入报告正文；详见 `docs/test-runbook.md`。

#### 3.2.4特征通过准则

已自动化覆盖的特征均以测试命令退出码为 0、断言全部通过作为通过准则。构建 warning 不判定为失败，但在风险中记录。

## 4测试用例说明

### 4.1 目的

本章列出本轮已执行测试用例及实际结果，作为测试日志和总结结论的依据。

### 4.2详细说明

#### 4.2.1 测试用例说明标识符

| 用例组 | 标识符 |
| --- | --- |
| 后端自动化 | `TM-BE-AUTO-20260606` |
| 前端 E2E | `TM-FE-E2E-20260606` |
| 构建与脚本 | `TM-BUILD-SCRIPT-20260606` |

#### 4.2.2 输入说明

| 输入类别 | 输入内容 |
| --- | --- |
| 后端测试输入 | MockMvc 请求、数据库现有种子数据、错误登录参数、匿名请求 |
| 前端测试输入 | Playwright 模拟 API 返回、匿名 localStorage、普通用户 localStorage、登录表单输入 |
| 构建输入 | `frontend` 源码、Vite 配置、npm 依赖 |
| 启动脚本输入 | 根目录 `.env`、`start.ps1 -DryRun` |

#### 4.2.3 输出说明

| 用例编号 | 用例名称 | 输出/实际结果 | 状态 |
| --- | --- | --- | --- |
| BE-01 | Spring Boot 上下文加载 | `BackendApplicationTests` 通过 | 通过 |
| BE-02 | 匿名发布游记拦截 | 返回 403 | 通过 |
| BE-03 | 匿名访问我的游记拦截 | 返回 403 | 通过 |
| BE-04 | 匿名访问关注流拦截 | 返回 403 | 通过 |
| BE-05 | 匿名删除游记拦截 | 返回 403 | 通过 |
| BE-06 | 匿名访问酒店订单拦截 | 返回 403 | 通过 |
| BE-07 | 匿名访问管理后台接口拦截 | 返回 403 | 通过 |
| BE-08 | 静态图片公开访问 | 返回 200 | 通过 |
| BE-09 | 部署前端 Origin CORS | 返回允许的 `Access-Control-Allow-Origin` | 通过 |
| BE-10 | 航班/火车/酒店/景点/社区/优惠券公开接口 | 返回 `code=200` 统一响应 | 通过 |
| BE-11 | 错误登录 | 返回 `code=500`，提示用户名或密码错误 | 通过 |
| BE-12 | 酒店/景点/AI 写接口匿名拦截 | 返回 403 | 通过 |
| BE-13 | `Result.success` 响应结构 | `code=200,msg=成功,data` 正确 | 通过 |
| BE-14 | `Result.error` 响应结构 | `code=500,msg` 正确，`data=null` | 通过 |
| BE-15 | 库存预扣减状态枚举 | 三种状态顺序和命名正确 | 通过 |
| FE-01 | 公开页面渲染 | 9 个公开路由可渲染 | 通过 |
| FE-02 | 匿名访问鉴权路由 | 跳转 `/login` | 通过 |
| FE-03 | 非管理员访问 `/admin` | 跳转首页 | 通过 |
| FE-04 | 登录流程 | 写入 `mock-token` 并返回首页 | 通过 |
| BUILD-01 | 前端生产构建 | 构建成功 | 通过 |
| SCRIPT-01 | 启动脚本 DryRun | 检测到 Redis 已监听，并输出后端/前端启动命令 | 通过 |
| AI-01 | AI 客服正常调用 | 登录测试用户后调用 `/api/ai/chat`，返回 `code=200`，回复内容非空 | 通过 |
| AI-02 | AI 行程正常生成 | 登录测试用户后调用 `/api/ai/plan/generate`，返回 `code=200`，`planContent` 为合法 JSON，包含 2 天行程、行前清单、风险提示和备选方案 | 通过 |
| AI-03 | AI 无有效 Key 降级 | 临时 18080 后端使用占位 Key 调用 AI 客服和行程生成，均返回 `code=200`，客服返回本地兜底回复，行程返回 3 天模板 JSON | 通过 |

## 5测试规程说明

### 5.1 目的

本章记录本轮测试规程的交付物和实际执行状态，便于后续复测。

### 5.2 详细说明

#### 5.2.1 测试规程说明标识符

`TM-TP-20260606-AUTO-RUNBOOK`

#### 5.2.2特殊要求

| 要求 | 本轮状态 |
| --- | --- |
| MySQL 数据库可连接 | 已满足，后端测试日志显示连接 `travelmate` 成功 |
| Playwright Chromium 可用 | 已满足，安装后 E2E 通过 |
| Redis 可用 | 16:09 回归脚本复测时已满足；16:52 压测前置检查时 `127.0.0.1:6379` 未监听 |
| DeepSeek API Key | `.env` 中被后端识别；已完成 AI 客服、AI 行程生成和无有效 Key 降级验证 |

#### 5.2.3 规程步骤

规程代码和命令入口已落地到：

| 文件 | 用途 |
| --- | --- |
| `scripts/run-tests.ps1` | 一键执行后端测试、前端安装、构建、E2E、启动脚本 DryRun |
| `docs/test-runbook.md` | 测试运行说明 |
| `frontend/playwright.config.js` | Playwright 配置 |
| `frontend/tests/e2e/travelmate-smoke.spec.js` | 前端 E2E 用例 |
| `backend/src/test/java/com/travelmate/PublicApiSmokeTests.java` | 后端公开接口和安全边界测试 |
| `backend/src/test/java/com/travelmate/ResultAndStockBoundaryTests.java` | 统一响应和库存状态边界测试 |

## 6 测试日志

| 时间 | 操作 | 结果 |
| --- | --- | --- |
| 2026-06-06 15:42 | 执行 `backend\mvnw.cmd test` | 15 条后端测试通过 |
| 2026-06-06 15:43 | 执行 `frontend\npm run build` | 构建成功，有大 chunk warning |
| 2026-06-06 15:43 | 执行 `npx playwright test` | 失败，原因是 Playwright Chromium 未安装 |
| 2026-06-06 15:51 | 执行 `npx playwright install chromium` | 安装成功 |
| 2026-06-06 15:53 | 执行 `npx playwright test --reporter=list --workers=1` | 4 条 E2E 全部通过 |
| 2026-06-06 15:57 | 执行 `.\scripts\run-tests.ps1` | 后端 15 条、前端构建、E2E 4 条、启动 DryRun 全部完成 |
| 2026-06-06 16:09 | Redis 启动后再次执行 `.\scripts\run-tests.ps1` | Redis 端口连通；后端 15 条、前端构建、E2E 4 条、启动 DryRun 全部通过 |
| 2026-06-06 16:38 | 执行 AI 客服接口连通测试 | 注册临时普通用户并登录，调用 `/api/ai/chat` 返回 `code=200`，AI 回复内容非空 |
| 2026-06-06 16:42 | 执行 AI 正常调用补测 | 使用有效 DeepSeek Key 调用 `/api/ai/chat` 和 `/api/ai/plan/generate`；客服返回预期短句，行程返回 `code=200`，`planContent` 为合法 JSON，包含 2 天行程、行前清单、风险提示和备选方案 |
| 2026-06-06 16:51 | 执行 AI 降级路径补测 | 临时启动 18080 后端并使用 `sk-demo-placeholder` 占位 Key；AI 客服返回本地兜底回复，AI 行程返回 3 天模板 JSON，接口均为 `code=200` |
| 2026-06-06 16:52 | 检查高并发库存压测前置环境 | 当前 `127.0.0.1:6379` 未监听，且真实下单压测会写入订单和扣减库存；本轮未执行真实库存压测 |
| 2026-06-06 17:10 | 执行云端公开页面真实浏览器兼容性补测 | 访问 `http://82.156.91.79:42356/`，桌面 1280x720 和手机 390x844 下首页、航班、火车、酒店、景点、社区、热门城市、关于、帮助均可渲染且无横向溢出；直接访问 `/login` 返回 403 空白，`/ai-plan`、`/coupons` 未登录时会跳转到 `/login` 后空白 |
| 2026-06-06 17:12 | 执行云端图片资源加载检查 | 公开页面多张 Wikimedia 外链图片被浏览器 ORB 拦截，社区、热门城市等页面存在外链图片加载失败风险；本轮未执行登录上传图片测试 |
| 2026-06-06 17:23 | 复测云端 SPA 路由回退 | 宝塔 Nginx 删除 `/login`、`/register` 后端代理后，直接访问 `/login`、`/ai-plan`、`/coupons` 均返回前端 `index.html`，不再 403 或返回后端 JSON |
| 2026-06-06 17:28 | 执行云端普通用户注册登录补测 | 通过登录页注册普通测试用户并登录成功；登录后 `/ai-plan`、`/coupons`、`/my-orders`、`/notifications`、`/collections` 均可渲染且无横向溢出 |
| 2026-06-06 17:29 | 执行云端管理员入口和后台访问补测 | 在空注册表单连续点击注册 3 次后弹出管理员注册，使用管理员密钥注册成功；管理员登录后 `/admin` 仪表盘可渲染，显示用户、订单、审核和日志统计 |
| 2026-06-06 17:38 | 执行云端内容审核闭环补测 | 普通用户发布游记后进入待审核；管理员接口可查看待审游记、敏感词列表，可人工通过/拒绝并写入拒绝原因；普通用户可在我的游记/通知中看到审核结果；评价举报创建后进入管理员举报工单，处理后状态变为已处理并保存处理备注；作者封禁/解封接口返回成功 |
| 2026-06-06 17:40 | 执行云端酒店库存 50 并发压测 | `http://82.156.91.79:42356/` 上 50 个临时用户并发抢同一酒店房型：`hotelId=6`、`roomId=14`，压测前库存 1；成功 1 单、库存不足失败 49 单，压测后库存 0，无负库存；成功订单已取消，最终库存回补为 1 |
| 2026-06-06 17:46 | 执行云端真实下单业务闭环补测 | 注册临时用户后完成乘客新增；航班、火车、酒店均完成“创建待支付订单 -> 取消回补库存”和“创建订单 -> 支付 -> 查询凭证 -> 发起退款申请”；首次景点购票因云端数据库缺少 `tm_attraction_order` 表失败，后续已补表并复测通过 |
| 2026-06-06 18:00 | 执行 Java 21 环境后端复测 | 安装并使用 Eclipse Temurin JDK 21.0.11，设置 `DB_PASSWORD=123456` 和 `SPRING_DATASOURCE_PASSWORD=123456` 后执行 `backend\mvnw.cmd test`，共 16 条测试通过，Spring Boot 日志显示使用 Java 21.0.11 启动 |
| 2026-06-06 18:01 | 执行 Java 21 环境启动脚本 DryRun | 使用 Java 21.0.11 执行 `.\start.ps1 -DryRun`，脚本正确识别数据库密码来源并输出后端、前端启动命令；当前本机 Redis 未监听，脚本按预期给出 Redis 警告 |
| 2026-06-06 18:02 | 执行云端 HTTP smoke 复测 | `http://82.156.91.79:42356/` 首页、`/api/post/list`、`/api/hotel/search`、`/api/attraction/search` 均返回 HTTP 200 |
| 2026-06-06 18:02 | 执行云端订单超时取消任务补测 | 创建酒店待支付订单 `HT1780739200026691A`，`roomCount=2`；房型库存从 11 扣减到 9，约 16 分钟后定时任务自动将订单状态置为 4，库存回补到 11 |
| 2026-06-06 18:20 | 补建景点订单表后复测云端景点购票 | 执行更新后的 `docs/sql/init.sql` 后，`tm_attraction_order` 表结构完整；景点购票返回订单号 `AT1780741216714BAC0`，订单列表和凭证可查询，景点库存从 9999 扣减到 9998 |

关键结果摘录：

| 命令 | 结果 |
| --- | --- |
| `backend\mvnw.cmd test` | `Tests run: 15, Failures: 0, Errors: 0, Skipped: 0` |
| Java 21 后端复测 `backend\mvnw.cmd test` | Temurin JDK 21.0.11 下 `Tests run: 16, Failures: 0, Errors: 0, Skipped: 0` |
| `npm run build` | `built in 27.48s`，退出码 0 |
| `npx playwright test --reporter=list --workers=1` | `4 passed` |
| `start.ps1 -DryRun` | 检测到 `Redis is already listening on 127.0.0.1:6379`，正确输出后端和前端启动命令 |
| Java 21 启动脚本 DryRun | Java 21.0.11 下正确输出后端和前端启动命令；当前 Redis 未监听时按预期提示警告 |
| `/api/ai/chat` 正常调用 | 登录后返回 `code=200`，回复内容非空 |
| `/api/ai/plan/generate` 正常调用 | 登录后返回 `code=200`，`planContent` 可解析为 JSON，包含 2 天行程、行前清单、风险提示和备选方案 |
| AI 降级路径 | 临时 18080 后端使用占位 Key，客服和行程接口均返回 `code=200`，未阻塞主流程 |
| 云端酒店库存 50 并发压测 | 50 个临时用户并发下单，成功 1 单、库存不足失败 49 单；压测前库存 1，压测后库存 0，取消成功订单后库存回补为 1；P50 296 ms、P95 381 ms、最大 385 ms |
| 云端真实下单业务闭环 | 航班、火车、酒店的创建、取消回补、支付、凭证和退款申请通过；补表后景点购票、订单列表、凭证查询和库存扣减通过 |
| 云端订单超时取消任务 | 酒店待支付订单 15 分钟后由调度任务自动取消，2 间房库存从 9 回补到 11，验证按 `roomCount` 回补 |

## 7测试总结报告

本轮已经完成测试代码补齐、自动化重测和报告重构。后端自动化测试由原有覆盖扩展为 15 条，前端新增 4 条 Playwright E2E smoke 测试，并通过 `scripts/run-tests.ps1` 完成统一回归。

### 测试结论

| 类别 | 结论 |
| --- | --- |
| 后端自动化 | 通过 |
| 前端 E2E | 通过 |
| 前端生产构建 | 通过 |
| 启动脚本 DryRun | 通过 |
| 核心安全边界 | 当前自动化覆盖项通过 |
| 真实下单业务闭环 | 已补测，通过 |
| 高并发压测 | 已补测，通过 |
| 外部 AI 正常/降级 | 已补测，通过 |
| Java 21 环境复测 | 已补测，通过 |

### 未测试/未完全覆盖项

| 编号 | 项目 | 当前状态 | 需要补测的内容 |
| --- | --- | --- | --- |
| NT-05 | 管理后台完整 CRUD | 部分补测 | 已验证管理员隐藏注册入口、管理员登录和 `/admin` 仪表盘访问；航班、火车、酒店、景点、城市资料、优惠券、用户管理、日志等新增/编辑/删除/分页/筛选仍需继续人工回归 |
| NT-07 | 文件上传与图片渲染 | 部分补测，未通过 | 云端公开页面发现多张 Wikimedia 外链图片被浏览器 ORB 拦截，存在裂图风险；社区图片上传、头像更新、本地 `/uploads/**` 仍需登录后继续验证 |
| NT-09 | 真实浏览器人工兼容性 | 部分补测，通过 | Chrome/Playwright 访问云端公开页面，桌面和手机宽度下主要公开页面可渲染且无横向溢出；修复 Nginx 配置后 `/login`、`/ai-plan`、`/coupons` 直接访问正常；普通用户登录后页面和管理员后台仪表盘可访问。Edge 真实浏览器和后台复杂操作仍需继续复核 |
| NT-11 | 外部 AI 长时间稳定性 | 未完全覆盖 | 已验证单次正常调用和无有效 Key 降级；长时间、多轮、大量请求、额度异常和超时场景仍建议最终验收前人工复核 |
| NT-12 | 扩展并发性能 | 未完全覆盖 | 已验证酒店单房型 50 并发无超卖；航班、火车、景点门票和更长时间阶梯负载可继续扩展压测 |

### 未通过/警告项

| 编号 | 项目 | 当前结果 | 处理状态 |
| --- | --- | --- | --- |
| WARN-01 | 首次 Playwright 执行 | 未通过，原因是 Chromium 浏览器二进制未安装 | 已执行 `npx playwright install chromium` 修复，复测 4 条 E2E 全部通过 |
| WARN-02 | 前端生产构建 | 构建通过，但 Vite 提示部分 chunk 超过 500 kB | 不影响本轮通过，建议后续做代码分包优化 |
| WARN-03 | Java 运行版本 | 已补测 Java 21.0.11；历史回归曾使用 Java 25.0.1 | Java 21 环境后端测试和启动脚本 DryRun 已通过，版本风险已关闭 |
| WARN-04 | 外部 AI 长时间稳定性 | 已补测单次正常调用和降级路径；未做长时间、多轮、大量请求稳定性验证 | 不影响本轮 AI 补测通过，最终验收前可继续人工复核额度、超时和多轮上下文 |
| WARN-05 | 高并发能力 | 已完成云端酒店库存 50 并发压测 | 单房型抢购未出现超卖；后续如需更完整性能报告，可继续扩展到航班、车次、景点门票和更长时间阶梯压测 |
| WARN-06 | 云端 SPA 路由回退 | 已修复复测通过；此前直接访问 `/login` 返回 403 空白，未登录访问 `/ai-plan`、`/coupons` 跳转后也会空白 | 已删除宝塔 Nginx 中 `/login`、`/register` 到后端的单独代理，保留 `try_files $uri $uri/ /index.html`，复测返回前端页面 |
| WARN-07 | 云端外链图片加载 | 多张 Wikimedia 图片被浏览器 ORB 拦截 | 建议替换为可跨域加载的图片源，或将关键图片下载到本地静态资源/对象存储 |

### 已发现风险

| 风险 | 影响 | 建议 |
| --- | --- | --- |
| 前端构建存在大 chunk warning | 首屏加载性能可能受影响 | 后续通过动态导入或 manualChunks 拆包优化 |
| Java 21 复测已完成 | 已使用 Temurin JDK 21.0.11 验证后端测试和启动脚本 DryRun | 保持项目交付环境使用 Java 21，避免再切回 Java 17 或 Java 25 作为最终验收版本 |
| 高并发压测范围有限 | 已验证酒店房型单资源 50 并发无超卖；尚未覆盖航班、车次、景点门票和长时间阶梯负载 | 按相同脚本扩展到其他库存资源，并在验收前保留压测原始输出 |
| 外部 AI 长时间稳定性未覆盖 | API Key 额度、网络抖动和长时间多轮对话未充分覆盖 | 最终验收前继续补测长时间对话、额度异常和超时场景 |
| 云端 `/login` 路由直接访问 403 | 已复测修复；历史风险为用户刷新登录页或从鉴权失败跳转到登录页时可能看到空白页 | 保持宝塔 Nginx 前端 history fallback 配置，不要恢复 `/login`、`/register` 到后端的单独代理 |
| 云端外链图片被拦截 | 社区、热门城市等页面可能出现图片无法显示 | 替换外链或使用本地/对象存储图片 |
| 管理后台完整 CRUD 未自动覆盖 | 管理端业务闭环仍需人工确认 | 最终答辩前按管理员角色人工回归 |

### 最终评价

当前版本的基础自动化回归、前端构建、页面 smoke、权限守卫和启动脚本 DryRun 均已通过；AI 客服、AI 行程生成和无有效 Key 降级路径已完成补测并通过。云端公开页面在桌面和手机宽度下大多可正常渲染，`/login` 直接访问 403 和未登录跳转登录页空白问题已通过宝塔 Nginx 配置修复并复测通过；普通用户登录后页面和管理员后台仪表盘也可访问。内容审核闭环已完成云端接口补测，覆盖待审、人工通过/拒绝、拒绝原因、审核通知、举报处理和封禁/解封作者。云端酒店库存 50 并发压测已补测通过，验证单房型抢购不会超卖且取消后可回补库存。真实下单链路中航班、火车、酒店已完成创建、取消回补、支付、凭证和退款申请补测；补建 `tm_attraction_order` 后景点购票、订单列表、凭证查询和库存扣减已复测通过。订单超时取消任务已验证酒店 `roomCount=2` 可自动取消并回补库存。系统具备课程验收演示的基础质量，后续重点建议放在 AI 长时间稳定性、后台完整 CRUD、文件上传与外链图片替换，以及多资源扩展并发复测。
